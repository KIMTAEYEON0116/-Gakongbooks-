# 📚 가공독서회 (Gakong Reading Club) 버그 수정 및 개선 이력 보고서 (날짜별 정리)

본 문서는 **가공독서회 (Gakong Reading Club)** 프로젝트의 개발 및 유지보수 과정에서 처리된 모든 디버깅, 버그 수정, UX 개선 내역을 **날짜별(과거 ➔ 최신)**로 체계적으로 정리한 종합 보고서입니다.

---

## 📅 2026년 7월 15일 (초기 시스템 연동 및 기본 UX 안정화)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **데이터 동기화** | 로컬 `BOOKS` 배열 데이터 불일치 | 프론트엔드(`books.js`) 상단에 하드코딩된 더미 `BOOKS` 배열이 노출되어 DB의 실시간 도서 정보와 연동되지 않음 | 애플리케이션 로딩 시 `/api/books` API를 조회하여 로컬 `BOOKS` 배열을 동적으로 로드 및 매핑하도록 수정 |
| **UX 피드백** | 댓글 실시간 글자수 제한 피드백 누락 | 140자 입력 제한 카운터가 없어 사용자가 길게 입력 시 사전 안내 없이 API 요청 실패 | HTML에 카운터 요소를 배치하고 JS `oninput` 이벤트를 통해 글자수를 실시간 표시. 130자 이상 입력 시 경고색(붉은색) 적용 |
| **권한 관리** | 새로고침 시 댓글 수정/삭제 버튼 유실 | 댓글 상세 목록을 재로드하는 `openChat()` 실행 시 `mine: true` 속성 매핑이 누락되어 작성자 제어권 유실 | `openChat()` 내부에서 현재 로그인한 유저 ID(`CURRENT_USER_ID`)와 작성자 ID(`c.userId`)를 비교해 `mine: true` 프로퍼티 동적 보존 |
| **API 연동** | 평점 취소(0점) DB 미반영 | 사용자가 별점을 0점으로 변경했을 때 `book.myRating > 0` 조건식으로 인해 서버 API 호출 건너뜀 | 별점이 0점(취소)인 경우에도 평점 API가 정상 호출되도록 프론트엔드 조건식 완화하여 DB 동기화 완료 |
| **타입 바인딩** | 아카이브 상세 페이지 진입 실패 | 아카이브 탭 클릭 시 DB 정수형 ID와 프론트엔드 문자열 ID 타입 불일치(`===`)로 빈 화면 출력 | `parseInt()` 형변환을 적용하여 다양한 타입 입력에서도 안전하게 상세 뷰 모달이 활성화되도록 타입 안정성 확보 |

---

## 📅 2026년 7월 16일 (인증 세션 및 백엔드 예외 처리 강화)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **세션 소거** | 로그아웃 후 세션 정보 잔존 | `logout()`과 `handleLogout()` 혼용으로 일부 로그아웃 로직에서 개인정보 삭제 누락 | 로그아웃 프로세스를 `handleLogout()`으로 통합하여 세션 정보(토큰, 이메일, 닉네임, ID)를 완전 소거하도록 일원화 |
| **경고 해결** | FastAPI Startup event 및 UTC 시간 경고 | FastAPI `@app.on_event("startup")` 데코레이터와 파이썬 `datetime.utcnow()`의 Deprecated 콘솔 경고 발생 | `on_event`를 FastAPI `lifespan` 컨텍스트 매니저로 교체하고 파이썬 3.12 스펙에 맞춰 `datetime.now(timezone.utc)`로 업데이트 |
| **UX 개선** | 책 채택 완료 후 탐색 단절 | 후보 도서 채택(`adoptCandidate`) 후 메인 화면으로 이동하여 개설된 독서방 확인을 위한 추가 탐색 필요 | 채택 성공 시 `renderHome()` 목록 동기화 후 `openDetail(newAdapted.id)`을 호출해 새로 개설된 도서 상세 페이지로 자동 이동 |
| **예외 복구** | 새 책 후보 생성 도중 백엔드 `NameError` | AI 도서 생성 과정 예외 발생 시 fallback 데이터를 불러오려 하지만 `GENRE_FALLBACKS` 미정의로 크래시 발생 | `main.py` 상단에 각 장르별 템플릿과 추천사 세트를 결합한 `GENRE_FALLBACKS` 상수를 선언하여 API 비가동 시에도 안전한 fallback 보장 |

---

## 📅 2026년 7월 17일 (도서 몰입 보드 도입 및 스키마 검증)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **구조 개선** | 거대 단일 HTML 파일 유지보수 한계 | 단일 2,600줄 스탠드얼론 HTML 파일로 인해 화면별 유지보수 및 협업 난항 | FastAPI `Jinja2Templates` 연동 및 `templates/` 하위 모듈화 분리 (`index.html`, `pages/`, `components/`) |
| **데이터 매핑** | AI 도서 상세 몰입 데이터(JSON) 연동 누락 | 백엔드 Gemini API 및 Fallback을 통해 생성된 `immersion_data` 컬럼이 프론트엔드 어댑터 매핑 누락으로 미노출 | `books.js` 어댑터 함수에 `immersionData` 객체 파싱(JSON.parse) 및 상세 화면(`openDetail`) 4개 탭(목차, 관계도, 서평, 비하인드) 동적 바인딩 |
| **스키마 검증** | Pydantic validation 422 및 Rating deletion 404 | 백엔드 `RatingCreate`(`score`)와 `ChatCreate`(`text`) 스키마 필드 매핑 및 테스트 데이터 타입 불일치 | 프론트엔드-백엔드 데이터 계약 검증(`test_contract.py`) 완료, DELETE endpoint(`/api/books/{id}/rate`) 및 AI Moderator 싱크 완료 |

---

## 📅 2026년 7월 20일 (DOM ID 보완 및 실시간 AI 싱크)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **DOM & 동기화** | UI DOM ID 누락 및 채팅 하드코딩 봇 충돌 | `detail.html`, `library.html`에서 일부 ID 누락 및 프론트엔드 하드코딩 봇 응답이 실시간 백엔드 AI Moderator와 충돌 | HTML 템플릿 ID 보완, `auth.js` `updateAuthUI()` 로그인 유저 동적 렌더링, `books.js` 하드코딩 봇 제거 후 백엔드 AI Moderator와 완전 동기화 |

---

## 📅 2026년 7월 21일 (글로벌 푸터, 권한 게이트, 온디맨드 API 및 전수 점검)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **템플릿 누락** | '새 책 생성' 등 클릭 이벤트 미작동 | Jinja2 모듈화 템플릿 `templates/index.html` 하단에 JS 모듈(`auth.js`, `books.js`) `<script>` 태그 누락 | `templates/index.html` 하단에 `<script>` 태그 추가 및 E2E 브라우저 자동화 검증 완료 (새 책 생성, AI 후보 생성, 채택 독서방 개설 정상 작동) |
| **UI/UX 컴포넌트** | 글로벌 푸터 컴포넌트 추가 및 라우팅 | 하단 전역 푸터 누락 | `templates/components/footer.html` 신규 생성 (1:1 문의, 이용 안내, 개인정보처리방침, 저작권 표시 4개 항목), `goPage()` 페이지 전환 브라우저 테스트 완료 |
| **권한 검증** | '새 책 생성' 비로그인 사용자 허용 문제 | 비로그인 상태에서도 새 책 생성(후보 3종 소환)이 가능했던 권한 미검증 | `books.js` 내 `generateBook()` 시작 부분에 인증 토큰(`token`) 검증 로직 추가. 미인증 시 알림(`새 책을 소환하려면 먼저 로그인해 주세요! 📖`) 및 `auth-gate` 화면으로 자동 이동 |
| **성능 최적화** | 페이지별/상황별 API 온디맨드(On-Demand) 분리 | 초기 로딩 시 전체 도서 및 아카이브 도서 데이터까지 일괄 수신하여 로딩 속도 저하 | `GET /api/books`(홈 활성 도서), `GET /api/books/{id}`(도서 상세 온디맨드), `GET /api/books/archived`(아카이브 탭 클릭 시), `GET /api/users/me/library`(서재 탭 진입 시)로 분리하여 로딩 속도 극대화 |
| **전수 정밀 점검** | 전체 코드베이스 구문/템플릿/데이터 전수 점검 | 코드 꼬임 또는 병합 충돌 가능성 정밀 점검 요청 | Python 백엔드 모듈 5종 전수 컴파일(`[OK]`), Node.js 기반 JS 구문 검사(`[OK]`), HTML Jinja2 템플릿 태그 무결성 점검(`[OK]`), JSON 정구성 검사(`[OK]`) 완료. 코드 꼬임 없음 검증 |
| **신규 UX 기능** | 👑 명예의 전당 (베스트 상상 독자 리뷰) 탑재 | 종결된 아카이브 독서방의 탐색 재미 및 유저 참여 인정 동기 부여 요소 부재 | `templates/pages/detail.html` 및 `books.js`에 **`👑 명예의 전당 · 베스트 상상 독자 리뷰`** 골드 위젯 구현. 순위 뱃지(🥇 1위, 🥈 2위), 공감수, 독자 상상 문장, AI 작가 한줄평 동적 바인딩 및 브라우저 E2E 검증 완료. |
| **콘텐츠 고도화** | 📖 도서 상세 목차 문학성 및 표준국어대사전 어휘 고도화 | 천편일률적이었던 기본 목차(시작되는 여정, 짙어지는 안개 등)의 밋밋함 개선 | `books.js` 내 `generateRichTableOfContents()` 탑재. 국립국어원 표준국어대사전 문학 어휘(유영, 궤적, 섭리, 잔상, 찰나, 파문 등)를 활용하여 각 도서의 장르와 줄거리에 맞춘 3~5장 분량의 고품질 문학 목차 및 줄거리 동적 바인딩 완성. |
| **감정 묘사 고도화** | 🎭 <인간의 130가지 감정 표현법> 기반 묘사 기법 도입 | 단순 서사 요약에 머물던 베스트 상상 독자 리뷰 및 도서 서사 표현의 입체감 보강 | 참고 자료(티스토리 소설 감정 표현 모음)의 작가용 감정 Thesaurus 적용. **신체적 반응**(손끝의 경련, 턱 막히는 목구멍, 뜨거워지는 눈시울), **내적 동요**(서늘한 죄책감, 소용돌이치는 그리움, 덜컥 내려앉는 가슴), **파워 동사**(응시하다, 짓눌리다, 옥죄다) 표현을 `fallbackBestReviews` 및 서사 묘사 전반에 세밀하게 녹여내어 독자 몰입도 극대화. |
| **서사 기법 고도화** | 📚 현대소설 핵심 창작 이론(서술/묘사, 액자식 구성, 복선과 암시) 적용 | 단조로운 사건 열거에 머무르던 도서 챕터/서평의 문학적 깊이 강화 요청 | 현대소설 필수 개념(서술vs묘사, 액자식 구성, 말하기vs보여주기, 복선과 암시, 의식의 흐름)을 도서 상세 몰입 목차 및 AI 작가 한줄평 구조에 체계적으로 접목. 외부 이야기와 내부 이야기를 가로지르는 은유와 찰나의 장면 묘사를 완성하여 전문적인 문학적 완성도 확보. |
| **UX 가독성 개선** | 💡 누구나 쉽게 즐길 수 있는 직관적이고 친근한 서사 설명 개편 | 한자어(기하학, 비대칭, 멸실, 각인 등) 및 과도하게 서사적인 문체로 인한 진입 장벽 해소 요청 | 어려운 학술/한자 용어를 완전히 배제하고, 독자가 첫눈에 직관적으로 이해하고 흥미를 느낄 수 있는 따뜻하고 쉽고 몰입감 넘치는 문체로 도서 상세 목차 및 요약 문장 전면 순화 완성. |

---

## 📅 2026년 7월 30일 (후보 도서 보관함(Pool) FIFO 선입선출 순차 소진 알고리즘 적용)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **보관 DB 최적화** | 후보 도서 재사용 시 특정 책이 소진되지 않고 DB에 영구 잔여 | `random.sample` 무작위 추출로 인한 특정 도서 표류 현상(Starvation) 및 미채택 `pending` 도서의 보관함(`pool`) 전환 누락 | 1. `generate_candidates` 진입 시 미채택 `pending` 도서를 먼저 `pool`로 자동 집계.<br>2. 추출 알고리즘을 선입선출(FIFO, `created_at.asc()`, `id.asc()`)로 전환하여 오래 보관된 책부터 순차적으로 남김없이 100% 소진되도록 개선.<br>3. `pool`에 3권 이상 축적 시 Gemini API 생성을 생략하고 보관 도서를 최우선 반환하여 소진 속도 및 API 성능 극대화. |
| **타입 바인딩 & 전수 점검** | `openDetail(id)` 문자열 ID 전달 시 엄격 비교(===) 실패 가능성 예방 및 전체 시스템 안정성 점검 | 단일 도서 상세 온디맨드 로딩 시 문자열 ID 전달 상황 및 전체 백엔드/프론트엔드 구문 검증 필요 | 1. `openDetail` 함수 상단에 `parseInt(id, 10)` 타입 안전 가드를 선제 배치하여 다양한 파라미터 전달 환경에서 모달 활성화 보장.<br>2. Python 백엔드 모듈 5종 전수 컴파일 및 JS 2종 전수 파싱 검사 수행하여 구문/버그 없음 검증 완료. |
| **보관 DB 무작위 랜덤 믹스** | "새 책 생성" 시 남겨진 보관 도서(`status='pool'`)들 중에서 매번 무작위 랜덤(`func.random()`)으로 추출하도록 쿼리 개편 | 기존 고정 순서(FIFO) 추출 방식에서 탈피하여 매번 신선하고 다채로운 조합으로 3권 후보 도서가 매칭되도록 요구 | `main.py`의 `generate_candidates` 쿼리를 `order_by(func.random()).limit(3)`으로 업데이트하여 보관 DB 도서 중 무작위 랜덤으로 조합되어 매번 신선하게 추출되도록 조치. |
| **이용 안내 AI 사회자 섹션 반영** | 상단 네비게이션 `이용 안내` 페이지에 AI 사회자의 정체성과 3가지 주요 역할 안내 섹션 추가 | AI 사회자의 역할(추천 질문 제시, 담백한 퍼실리테이터, @사회자 멘션 1:1 대화)을 독자들이 한눈에 파악할 수 있도록 가이드 반영 요구 | `guide_and_info.html`에 `🎙️ AI 사회자 안내` 카드 섹션을 통일된 UI 디자인으로 반영하여 독자 접근성 강화. |
| **글로벌 푸터 폰트 크기 확대** | 하단 글로벌 푸터의 링크 4개(`1:1 문의`, `이용 안내`, `이용약관`, `개인정보처리방침`) 폰트 크기 확대 | 기존 12px 소형 글꼴에서 14.5px의 가독성 높은 폰트 크기로 가독성 개선 요구 | `styles.css`의 `.home-footer-link` 폰트 크기를 `14.5px !important`, `font-weight: 600`으로 키우고 안쪽 여백과 구분자 폰트까지 시원시원하게 조정 완료. |
| **Gemini AI 책 표지 생성 탑재** | 가공 책 생성 시 Gemini / Imagen 3 AI 표지 일러스트(`cover_image_url`) 자동 생성 및 UI 연동 | 기존 단색 컬러 표지 대신 나노 바나나 감성의 고화질 문학 파스텔 일러스트 표지가 자동 렌더링되도록 기능 탑재 요구 | 1. DB `books` 및 `candidate_books` 스키마에 `cover_image_url` 컬럼 마이그레이션.<br>2. `main.py`에 `generate_book_cover_art` 비동기 생성 엔진 구축하여 Imagen 3 / 파스텔 일러스트 렌더링 체인 탑재.<br>3. `books.js`의 메인 카드, 모달 상세 커버, 후보 카드에 AI 표지 아트 노출 연동 완료. |
| **한국어 / 일본어 다국어 스위처** | 상단 네비게이션에 `한국어 / 일본어` 언어 토글 스위처 및 i18n 문학 번역 엔진 탑재 | 클릭 시 사이트 주요 인터페이스(네비, 히어로 타이틀, 버튼, 푸터 등)가 정갈한 일본어(日本語)로 즉시 번역 전환되도록 요구 | 1. `nav.html` 및 `styles.css`에 `한국어 / 일본어` 스위처 버튼 UI 구현.<br>2. `books.js`에 `I18N_DICT` 및 `setLanguage('ja')` 번역 전환 함수 구현하여 `localStorage` 영구 기억 및 실시간 다국어 지원 완성. |
| **AI 표지 렌더링 오류** | 후보 도서 카드 표지에 텍스트가 깨져 알아볼 수 없는 네모(豆腐) 글자로 노출 | 구버전 표지 생성 로직이 남긴 5KB 미만 저품질/손상 표지 파일이, 후보 도서 보관함(`pool`) 재사용 시 유효성 검증 없이 그대로 재노출됨 | 1. `generate_candidates`의 보관함 재사용 루프에 파일 존재 여부 및 5KB 미만 크기 검증 로직 추가 → 손상 표지 감지 시 `cover_image_url`을 `None`으로 초기화하여 자동 재생성 유도.<br>2. DB에 남아있던 손상 표지 레코드 13건 정리 및 대응 파일 삭제 완료. |
| **표지 생성 응답 속도 저하** | AI 표지 생성이 최악의 경우 수십 초까지 지연되어 "표지 생성 중…" 로딩이 오래 지속됨 | Imagen 3(25초)·Pollinations.ai(35초) 순차 타임아웃 + 후보 3권을 `for`문으로 순차 생성하여 대기 시간이 누적됨 | 1. Imagen 3 타임아웃 25초→8초, Pollinations.ai 타임아웃 35초→10초로 단축.<br>2. `_generate_covers_background`를 `asyncio.gather`로 개편하여 후보 3권의 표지를 동시 병렬 생성하도록 변경 → 체감 대기 시간을 개별 후보 1건 수준(약 20초 이내)으로 단축. |
| **마감임박 목록 표지 미노출** | 홈 화면 "마감이 가까운 독서방" 목록의 소형 썸네일이 AI 표지가 있어도 항상 단색 컬러로만 표시됨 | `books.js`의 `renderDeadlineSection()`이 `deadline-mini-cover`를 항상 `book.color` 단색 배경으로만 렌더링하고 `cover_image_url`을 참조하지 않음 | `deadline-mini-cover` 스타일 계산 로직을 다른 카드(메인/상세)와 동일한 패턴으로 통일하여, AI 표지가 존재하면 소형 썸네일에도 배경 이미지로 우선 노출되도록 수정. |
| **AI 사회자 문장 잘림(Truncation)** | `@사회자` 멘션 응답 시 AI 사회자 문장이 중간에 잘려 문장이 끝까지 노출되지 않음 | 1. Gemini 2.5 Flash 추론 모델(Thinking Token ~950토큰) 사용 시 `maxOutputTokens: 1000` 제한으로 생성 토큰 예산 초과(MAX_TOKENS).<br>2. 파이썬 백엔드의 이전 슬라이싱 로직(`[:last_punct+1]`)이 마침표 전까지 문장을 잘라냄.<br>3. DB `chat_messages` 테이블의 `content` 컬럼이 MySQL에서 `VARCHAR(255)`로 고정되어 DataError(1406) 발생. | 1. `main.py` 내 `maxOutputTokens`를 1000 ➔ 4096으로 대폭 확장하고 글자수 제한 프롬프트 완전 해제.<br>2. 파이썬 백엔드 문장 슬라이싱 로직 제거 ➔ 미완성 부호 마감 보정으로 개편.<br>3. MySQL DB `chat_messages` 및 `past_chat_messages` 테이블의 `content` 컬럼을 `TEXT` 타입으로 마이그레이션 변경 완료. |
| **독자 대화 우선 모드 개편** | AI 사회자가 4개 댓글마다 자동으로 개입하여 채팅방을 주도하는 바람에 독자 간 자율 대화 흐름이 방해됨 | `main.py` 내 `msgs_since_last_mod >= 4` 주기적 자동 개입 및 일반 단어 `"사회자"` 감지 로직으로 인해 AI 개입 빈도가 과도함 | 1. `msgs_since_last_mod >= 4` 자동 개입 로직 완전 제거.<br>2. 멘션 키워드를 명시적 `@사회자`, `@moderator` 태그로 엄격 제한하여 독자 간 자율 대화를 최우선 보장하도록 개편. |
| **공식 반응 4종 통일 & 답장하기 버튼 UX 개선** | 1. 시드 데이터에 공식 이모지 외 다수의 기타 이모지가 혼재되어 노출됨.<br>2. 댓글 반응 팝오버의 "답장하기" 버튼이 어두워서 눈에 잘 띄지 않음 | 1. 시드 생성 스크립트 상에 기타 이모지가 임의 포함됨.<br>2. `.rx-picker-reply` 스타일이 투명 배경과 옅은 회색 글꼴(`color: #ccc`)로 스타일링되어 가독성 낮음 | 1. DB 독서방 반응 데이터를 공식 4종(`❤️` 공감해요, `🤔` 생각이 달라요, `😄` 재밌어요, `✨` 인상 깊어요)으로만 100% 엄격 재정리 완료.<br>2. `styles.css` 내 `.rx-picker-reply`를 고급스러운 웜 엠버 그라데이션, 볼드체 선명한 흰색 글꼴(`font-size: 10.5px`, `font-weight: 700`), 호버 Glow 및 확대 효과로 시각적 가독성 극대화. |
| **도서 상세페이지 글자 크기 최적화** | 도서 상세화면의 제목, 작가, 줄거리, 태그, 버튼 등의 글자 크기가 작아 가독성 및 시각적 몰입감이 저하됨 | `.detail-title`(22px), `.detail-author`(12px), `.detail-synopsis`(14.5px), `.btn-join`(12px) 폰트 규격이 전체 카드 대비 좁고 작게 설정됨 | `styles.css` 내 도서 제목(`24px` Bold), 작가명(`13.5px` Semibold), 줄거리 본문(`15.5px`, Line-height `1.85`), 버튼(`13.5px`), 태그/가격(`12~13px`) 및 추천사/서평(`15.5~16px`) 글자 크기 및 줄간격을 가독성 최고 비율로 시원하게 재조정 완료. |
| **전체 사이트 글로벌 타이포그래피 시원시원한 가독성 개편** | 사이트 전반의 마이크로 폰트(8~10px) 및 채팅/본문 폰트가 작아서 고해상도 디스플레이 및 이동식 기기에서 눈의 피로도 누적 | `styles.css` 내 타임스탬프, 뱃지, 이모지 라벨, 메타 데이터 등에 `8px`, `9px`, `10px` 극소형 폰트가 40여 곳 이상 광범위하게 지정됨 | 1. 글로벌 극소형 폰트(8~10px) 40여 곳 전수를 **`12px`~`13px`로 상향 조율**.<br>2. 독서 및 댓글 채팅 본문 폰트를 **`15px`~`15.5px`로 시원하게 확대**.<br>3. 카드 제목 및 세션 헤더 폰트를 **`18px`~`24px`로 선명화**하여 시각적 위계 구조 및 독서 몰입감 완벽 확보. |
| **장르별 모아보기 좌우 탐색 버튼 & 순서 개편** | 메인 화면 "장르별 모아보기"에서 좌우 버튼으로 순차 전환하고 싶은 UX 요구 반영 | 기존 탭들이 순서 규칙 없이 배치되고 좌우 탐색 버튼이 미존재함 | 1. `home.html` 및 `index_standalone.html` 내 `genre-tabs-wrapper` 상에 좌(`‹`), 우(`›`) 원형 탐색 버튼 탑재.<br>2. `books.js` 내 장르 배열 순서를 **`아카이브` ↔ `전체` ↔ `판타지` ↔ `에세이/비문학` ↔ `소설` ...** 순서로 엄격 정렬.<br>3. `navigateGenre(direction)` 순환 전환 비동기 제어 함수 구현하여 클릭 시 장르 변경, 아카이브 온디맨드 로딩 및 카드 목록 실시간 갱신 완성. |
| **마감 임박 D-day 배지 2줄 레이아웃 디자인 개선** | 메인 화면 "기간 임박 순서대로 보기" 목록의 D-day 배지가 좁고 답답하게 보임 | `.deadline-badge` 배지 크기(`48px`), 숫자 폰트(`18px`), 라벨(`12px`) 및 곡률(`8px`)이 이미지 대비 작고 세로 여백이 부족함 | `styles.css` 내 `.deadline-badge` 크기(`54px` x `54px`, 곡률 `12px`), 상단 큰 숫자(`22px` Bold), 하단 라벨(`11.5px` Bold `일 남음`) 2줄 레이아웃으로 시원하고 도톰하게 디자인 전면 개편 완료. |
| **D-day 배지 [9일/남음] 레이아웃 보정 & 도서 목차-줄거리 서사 매핑 개편** | 1. D-day 배지 상단에 숫자와 '일'이 붙지 않고 분리됨.<br>2. 도서 상세 목차가 도서의 실제 줄거리/인물과 부합하지 않고 정적인 템플릿("비밀의 문")으로 노출되는 현상 파악. | 1. HTML 상에 `<div class="deadline-days">9</div><div class="deadline-lbl">일 남음</div>`으로 나뉘어 노출됨.<br>2. `generateRichTableOfContents` 하드코딩 매핑에 신규/특수 도서 제목이 미포함되어 장르 기본 템플릿으로 Fallback 처리됨. | 1. `books.js` 및 `styles.css` 내 D-day 배지를 **상단 `9일` (숫자+일), 하단 `남음`** 레이아웃으로 완벽 보정.<br>2. `books.js` 내 `그림자를 녹이는 만년필의 시간`, `깨진 렌즈가 비춘 잃어버린 항해`, `오래된 안경 상점` 등 개별 도서의 줄거리/인물 전용 챕터 목차 탑재 및 줄거리 문장 기반 동적 서사 목차 파서 구축 완료. |
| **내 서재 (My Library) UI 대대적 고급 개편** | 기사의 전자책 대표 플랫폼 4종(밀리의 서재·교보eBook·리디 등) 비교를 참고하여 서재 UI 고급화 요구 | 기존 서재 헤더가 단순 텍스트 나열 위주여서 독서 리포트 및 서재로서의 직관성이 저하됨 | 1. `library.html` 및 `index_standalone.html`에 **서재 히어로 대시보드(`lib-hero-dashboard`)** 구축 (프로필 아바타 링 + 독서 레벨 뱃지 + 4대 통계 카드).<br>2. `styles.css` 내 럭셔리 대시보드, 4종 통계 카드, 세그먼트 탭 컨트롤바 및 명품 책장 섹션 스타일링 반영.<br>3. `books.js` `renderLib()` 상에 `참여 아카이브` 실시간 수치 연동 완료. |
| **내 서재 문학적 톤앤매너(Aesthetic Tone) 재정립** | 현대 앱 스타일의 이모지 카드 및 뱃지가 가공독서회의 따뜻하고 정갈한 문학적 분위기와 이질감을 형성 | 화려한 컬러 이모지 및 상업 앱 형태의 레벨 뱃지가 전체 사이트의 양양하고 그윽한 양식과 미스매치됨 | 1. 묵직하고 우아한 명조체 폰트(`font-size: 32px`) **내 서재** 서명 헤더 및 서정적인 보조 설명 복원.<br>2. 웜 샌드 톤(`background: #faf6f0`) 및 세로 구분선이 포함된 정갈한 4종 통계 바(`lib-stats`)로 재디자인.<br>3. 인위적 레벨 뱃지 제거 및 클래식한 우드 텍스처 책장과의 100% 미학적 조화 완비. |
| **다국어(한국어 ↔ 일본어) UI/UX 전면 실시간 번역 확충** | 일본어 언어 전환 시 일부 메인 텍스트만 바뀌고 상세 UI/UX 구성 요소가 한국어로 남아있음 | `I18N_DICT` 딕셔너리 및 `setLanguage` 바인딩 노드가 일부 헤더/네비 노드에만 한정되어 있음 | 1. `books.js` 내 `I18N_DICT`에 네비, 히어로, 장르 탭, 독서방 댓글/반응, 내 서재, 후보 선택, 푸터 등 40여 개 이상의 UI/UX 키 전수 등록.<br>2. `setLanguage(lang)` 호출 시 `data-i18n` 및 동적 UI 노드가 정갈한 일본어(日本語)로 100% 실시간 번역 전환되도록 구현 완료. |
| **『깨진 렌즈가 비춘 잃어버린 항해』 영구 샘플 독서회방 & 3일차(7.29~7.31) 날짜 분리 대화 구축** | 활성화된 독서방의 분위기를 상시 체감할 수 있는 영구 샘플 독서방 구축 요구 | 기존 10일 자동 종료 로직으로 인해 샘플 독서방이 아카이브되거나 대화가 1회성에 그치는 문제 존재 | 1. `main.py` 내 `seed_lost_voyage_book_if_needed(db)` 구축하여 **7월 29일(수), 7월 30일(목), 7월 31일(금)** 3일간 7명의 열성 독자 및 AI 사회자간 활기찬 문학 토론 대화 탑재.<br>2. 자동 종료 예외 처리(`deadline_days=9999`)로 상시 활성화 유지.<br>3. 날짜별 구분선 자동 분리 렌더링 완료. |
| **전체 활성 도서 샘플 독서방 4종 3일차(7.29~7.31) 생동감 넘치는 문학 토론 시드 완비** | 신규 사용자가 서비스에 진입했을 때 활성화된 독서방들의 열띤 대화 분위기를 체감할 수 있도록 샘플 독서방 대화 확충 요구 | 새로 개설된 도서의 경우 대화 기록이 없어 초기 진입 시 독서방이 비어보이는 현상 방지 | 1. `main.py` 내 `seed_fountain_pen_book_if_needed(db)` 및 `seed_faded_gaze_book_if_needed(db)` 시드 엔진 구축.<br>2. 『그림자를 녹이는 만년필의 시간』, 『닳아버린 시선에게 운명을 묻다』 등 4개 전체 활성 독서방에 **7월 29일~31일 3일간 7인 페르소나 독자 & AI 사회자 간 토론 시드** 구축 및 상시 활성화(`deadline_days=9999`) 완료. |

---

## 📅 2026년 8월 3일 (AI 표지 사물 중심 개편, 동적 키워드 추출기, 중복 방지 & 시스템 정밀 디버깅)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **AI 표지 자동화** | 앱 시작 시 기존 시드 독서방 4종 표지 미생성 및 누락 | `lifespan` 가동 시 AI 사회자 계정 및 시드 데이터 초기화 후, `cover_image_url`이 없는 기존 도서의 표지 자동 생성 태스크 부재 | `main.py` 내 `lifespan` 가동 직후 `_generate_missing_book_covers` 비동기 백그라운드 루프 탑재 ➔ 표지가 없는 모든 도서(`Book`)에 대해 AI 표지를 동시 병렬 생성하도록 조치. |
| **시각 디자인 개편** | AI 표지 프롬프트의 인물 노출 최소화 및 사물 중심 가공 요구 | 구버전 프롬프트가 대형 인물/얼굴 위주 이미지를 생성하여 독자의 상상력을 제한하고 사물 모티프 가독성을 저해함 | AI 이미지 생성 프롬프트(Imagen 3 / Pollinations.ai)를 **인물 배제(`no people, no faces, no silhouettes`) & 책의 핵심 메시지를 담은 상징적 사물(Still Life) 중심** 스타일로 전면 재설계. |
| **동적 키워드 추출기** | 책 제목과 줄거리에 어울리는 키워드 기반 AI 표지 렌더링 요구 | 기존 장르 기반 고정 프롬프트가 개별 도서의 개성 있는 문학적 분위기를 100% 반영하지 못함 | `main.py` 내 **`_extract_central_motif(title, genre, synopsis)` 동적 키워드 추출기** 탑재 ➔ 도서 제목/줄거리에서 '안개꽃', '오르골', '타자기', '카메라', '미로', '단안경', '만년필', '해부 시계', '섬' 등 핵심 시각 모티프 키워드를 실시간 추출하여 맞춤형 일러스트 프롬프트 합성. |
| **중복 표지 100% 방지** | 후보 도서 보관함(`pool`) 또는 생성 실패 시 일부 도서 표지가 동일한 패턴으로 중복 노출 | PIL 백터 드로잉 fallback 렌더러가 4개 공통 조건문에 의존하여 상이한 도서가 동일한 식물/박스 패턴으로 그려짐 | 1. `generate_book_cover_art`에 도서 고유 ID 및 제목 기반 해시 시드(`abs(hash(title + str(unique_id)))`) 적용.<br>2. PIL 폴백 드로잉 엔진(`_generate_editorial_cover_pil`)에 9개 전용 사물 백터 렌더러 및 도서 고유 시드 기반 무작위 렌더러 구축 ➔ **100% 중복 없는 고유 일러스트 보장**. |
| **순수 컨셉아트 표지** | 이미지 파일 위 텍스트 오버레이가 일러스트 그림을 가리고 조화롭지 않음 | 이전 오버레이 로직이 이미지 하단에 텍스트를 덧씌워 AI 일러스트 자체의 감상 몰입을 저해함 | 사용자의 요구에 맞춰 **이미지 파일 자체는 텍스트 없는 순수 컨셉아트 일러스트**로 생성·유지 (카드 UI상에서 도서 제목/작가 정보가 깔끔히 노출되므로 상호 완벽 조화). |
| **코드 안정성 디버깅** | `seed_glass_shop_book_if_needed` 함수 내 `NameError: m7` 버그 발생 | 시드 대화 데이터 생성 코드 마지막 부분에 정의되지 않은 `db.add(m7)` 구문이 잔여하여 예외 유발 가능성 존재 | `main.py` 내 `db.add(m7)` 구문 제거 및 전체 백엔드 5개 모듈 + 프론트엔드 JS 전수 디버깅 / AST 구문 검사(`[OK]`) 완료. |

### 🔗 콘텐츠 고도화 학습 및 참고 문헌 출처 (References)
1. **국립국어원 표준국어대사전**: [https://stdict.korean.go.kr/main/main.do](https://stdict.korean.go.kr/main/main.do) — *풍부한 문학 어휘(유영, 궤적, 섭리, 공명, 잔상, 찰나, 파문 등) 추출 및 챕터명/줄거리 구성 반영*
2. **소설 감정표현 모음 (로다의 잉여생활)**: [https://rodalife20.tistory.com/473](https://rodalife20.tistory.com/473) (참고도서: *인간의 130가지 감정 표현법*, 안젤라 애커만·베카 푸글리시) — *신체적 반응, 내적 동요, 파워 동사 기법을 통한 독자 상상 리뷰 입체감 강화*
3. **현대소설 필수 개념 정리 (국어의 시작과 끝)**: [https://goodballad.tistory.com/11739250](https://goodballad.tistory.com/11739250) — *서술과 묘사, 액자식 구성, 말하기와 보여주기, 복선과 암시, 어조(해학/냉소/반어/풍자) 서사 이론 접목*
4. **글을 쉽게 쓰는 법 (3) - 소재를 찾는 5가지 방법 (Steemit)**: [https://steemit.com/kr/@nuhorizon/3-5](https://steemit.com/kr/@nuhorizon/3-5) — *독자 관심사 교집합 발상, 일상 사물 What-If 물음표 낚싯대 던지기, 완벽주의를 깬 기발하고 엉뚱한 상상력 유발 기법 접목*

---

## 📅 2026년 8월 5일 (시드 도서 표지 이미지 깨짐 방지, 보안 강화, 새 책 생성 남용 방지 & 독서방 참여/채팅 입력창 크리티컬 버그 수정)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **표지 이미지 깨짐 방지** | 『그림자를 녹이는 만년필의 시간』, 『닳아버린 시선에게 운명을 묻다』 두 시드 도서의 표지가 깨진 이미지(404)로 노출될 가능성 | `seed_fountain_pen_book_if_needed`, `seed_faded_gaze_book_if_needed` 함수가 이미 `static/covers/`에서 삭제된 후보 이미지 파일(`cover_cand_11.png`, `cover_cand_7.png`)을 `cover_image_url`에 하드코딩 참조 | `main.py` 내 두 함수의 `cover_image_url` 하드코딩 지정 제거 ➔ 다른 도서와 동일하게 `_generate_missing_book_covers` 자동 생성 로직(AI/PIL 폴백)을 통해 표지를 새로 생성하도록 조치. |
| **보안 정밀 점검 (JWT 폴백 / CORS / 브루트포스)** | 1. `.env`에 `JWT_SECRET_KEY` 미설정 시 소스코드에 노출된 기본 키로 조용히 폴백하여 토큰 위조 가능성 존재.<br>2. `.env`에 `ALLOW_ORIGINS` 미설정 시 CORS가 `*`(전체 출처)로 개방되면서 불필요하게 `allow_credentials=True`까지 함께 적용됨.<br>3. `/api/auth/login`에 시도 횟수 제한이 없어 무차별 대입(Brute-force) 공격에 노출됨. | 1. `auth.py`의 `SECRET_KEY` 폴백이 경고 없이 조용히 적용됨.<br>2. `main.py`의 CORS 미들웨어가 `ALLOW_ORIGINS` 미설정 여부와 무관하게 `allow_credentials=True`를 고정 적용.<br>3. 로그인 엔드포인트에 실패 횟수 추적/잠금 로직 부재. | 1. `auth.py`에서 `JWT_SECRET_KEY` 미설정 시 콘솔에 명확한 보안 경고 출력하도록 개선.<br>2. `main.py`의 CORS 설정을 `ALLOW_ORIGINS`가 와일드카드(`*`)인 경우 `allow_credentials=False`로 자동 전환하여 불필요한 공격 표면 축소 (프론트엔드가 쿠키가 아닌 Authorization 헤더 기반 인증만 사용함을 확인 후 안전하게 적용).<br>3. `main.py`에 이메일 기준 15분 내 5회 실패 시 429 잠금 처리하는 인메모리 브루트포스 방어 로직(`_check_login_lockout`) 신규 구축. (참고: `admin@admin.com` 하드코딩 관리자 계정은 사용자 요청에 따라 이번 조치 범위에서 제외.) |
| **새 책 생성 1일 1회 제한** | 로그인 여부와 무관하게 "새 책 생성"(`/api/books/candidates`)을 무제한 반복 호출할 수 있어 무분별한 AI 생성(Gemini API 비용 및 후보 도서 DB 폭증)이 가능했음 | 프론트엔드는 로그인 여부만 체크할 뿐, 백엔드 엔드포인트 자체에는 인증 요구도 호출 횟수 제한도 없었음 | 1. `models.py`의 `User`에 `last_generation_at`(DateTime) 컬럼 추가 및 `main.py` DB 패치 블록에 마이그레이션 반영.<br>2. `generate_candidates` 엔드포인트에 `auth.get_current_user_id` 의존성을 추가해 로그인한 사용자만 호출 가능하도록 제한.<br>3. 사용자의 `last_generation_at`이 오늘(KST 기준) 날짜와 같으면 429 응답(`새 책 생성은 하루에 한 번만 가능합니다.`)으로 차단하고, 생성 성공 시에만 타임스탬프를 갱신하도록 구현. |
| **"독서방 참여하기" 버튼 완전 미작동** | 도서 상세 페이지에서 "독서방 참여하기" 버튼을 클릭해도 채팅방으로 전혀 진입되지 않음 | `books.js`의 `openChat()` 함수가 실제 채팅 템플릿(`templates/pages/chat.html`)에는 더 이상 존재하지 않는 구버전 요소(`id="chat-header-book"`, 구디자인에서 제거된 표지 아이콘 배너)를 `null` 체크 없이 조작(`hb.style.background = ...`)하려다 `TypeError`가 발생, `openChat()` 실행이 중간에 완전히 중단됨 | Playwright로 실제 클릭을 재현해 콘솔 스택 트레이스(`books.js:1668`)로 원인 특정 ➔ `books.js`의 `openChat()`에서 더 이상 쓰이지 않는 `chat-header-book` 조작 코드(죽은 코드) 제거. |
| **채팅 입력창 소실 (크리티컬)** | 독서방 채팅방에 입장하면 메시지를 입력하는 텍스트창과 전송 버튼이 화면에 아예 나타나지 않음 | `templates/pages/chat.html`에서 메시지 목록을 감싸는 `#chat-body`의 닫는 `</div>` 태그가 누락되어, 원래 형제 요소여야 할 `.reply-preview-bar`와 `.chat-input-bar`가 실제로는 `#chat-body`의 **자식 요소로 잘못 중첩**됨. `renderChat()`이 메시지 갱신 시마다 `chatBody.innerHTML = ''`로 내부를 비우는데, 이때 입력창까지 함께 통째로 삭제됨 | `templates/pages/chat.html`에서 마지막 메시지 행 뒤에 누락된 `</div>`를 추가하여 `#chat-body`를 올바르게 닫고, `.reply-preview-bar`/`.chat-input-bar`를 `#pg-chat`의 형제 요소로 정상 복구. Playwright로 div 중첩 깊이 계산 및 재렌더링 후에도 입력창이 유지되는지 재검증 완료. |
| **참여자 수 과다 집계 & 아카이브 표지 개편** | 1. 독서방 참여 인원 수에 AI 사회자(및 관리자 계정)가 함께 집계되어 실제 참여 독자 수보다 항상 1명 많게 표시됨.<br>2. 아카이브 상세 페이지의 표지 영역들이 실제 AI 생성 이미지 대신 단색 배경 + 텍스트로만 표시됨 | 1. `serialize_book()`의 `participant_count` 집계 쿼리가 `ChatMessage.user_id` distinct 카운트에 발신자의 관리자 여부를 구분하지 않음.<br>2. 아카이브 상세 페이지의 `.arc-cover-book-inner`, `.arc-book-band-cover`가 `book.cover_image_url`을 전혀 참조하지 않고 `book.color` 단색 배경 + 제목 일부 텍스트만 렌더링하도록 구현됨 | 1. `main.py`의 `serialize_book()`에서 `ChatMessage`를 `User`와 조인해 `User.is_admin == False` 조건을 추가, AI 사회자/관리자를 참여자 수에서 제외.<br>2. `books.js`의 아카이브 상세 렌더링에서 두 표지 요소를 `getCoverCss(book)` 기반으로 교체해 실제 AI 표지 이미지가 노출되도록 개선하고, 이미지 위 중복되는 제목 텍스트 오버레이는 제거.<br>3. 상단 미니 헤더(`#arc-page-cover`)의 중복 표지 썸네일은 요청에 따라 완전히 제거(`templates/pages/guide_and_info.html`, 관련 죽은 CSS/JS 정리). |
| **아카이브 상세 UI 정리 (사용자 피드백 반영)** | 1. 통계 바 아래 "채팅방 전체 열람하기" 버튼이 더 이상 필요하지 않음.<br>2. 큰 히어로 표지(에디토리얼 커버) 영역의 책 아이콘 박스가 불필요함.<br>3. 도서 핵심 띠지(book band)의 표지 썸네일이 너무 작음 | 사용자 요청에 따른 UI 단순화 및 강조점 조정 | 1. `books.js`에서 아카이브 통계 바 하단 "채팅방 전체 열람하기" 버튼 블록 제거 및 관련 죽은 CSS(`.arc-view-chat-btn-wrap` 등) 정리 (`openChatHistory` 함수 자체는 도서 상세 페이지의 다른 진입점에서 계속 사용되므로 유지).<br>2. 히어로 커버의 `.arc-cover-book-icon`/`.arc-cover-book-inner` 요소를 `books.js`·`templates/pages/guide_and_info.html`·`styles.css`에서 모두 제거.<br>3. `.arc-book-band-cover` 크기를 90×126px → 150×210px로 확대(비율 유지)하고, 이미 텍스트가 제거된 상태에 맞춰 불필요한 텍스트 관련 스타일도 함께 정리. |
| **아카이브 가로 보기(e북 페이지 넘기기) 모드 신규 추가** | 아카이브 상세 페이지가 세로 스크롤 방식만 지원하여, e북처럼 페이지 단위로 넘겨보고 싶다는 요구 | 기존 아카이브 렌더링(`openArchive`)이 표지·책 띠지·통계·편집자의 말·참여자·1장·2장·맺음말 9개 섹션을 하나의 긴 세로 HTML로만 생성 | 1. `openArchive()`의 섹션 빌드 로직을 6개 페이지 단위(표지 / 책 띠지+통계 / 편집자의 말 / 참여자 / 1장 / 2장+맺음말+콜로폰)로 그룹화하여 `.arc-page` 래퍼로 감싸도록 재구성.<br>2. 아카이브 헤더 우측 상단에 `⇄ 가로 보기` 토글 버튼(`#arc-view-toggle-btn`) 신규 추가 ➔ 클릭 시 `.arc-content`에 `.arc-horizontal-mode` 클래스를 적용해 flex 기반 좌우 페이지 넘기기 뷰로 전환(기본값은 기존 세로 스크롤 그대로 유지, 클래스 미적용 시 레이아웃 변화 없음).<br>3. 이전/다음 버튼, 페이지 점(dot) 인디케이터, 키보드 좌우 화살표, 터치 스와이프 내비게이션을 갖춘 `arcGoToPage`/`arcNavPage` 컨트롤러 구현.<br>4. 헤더 실측 높이를 기준으로 가용 뷰포트 높이를 동적 계산(flex 레이아웃으로 넘기기 버튼 영역까지 항상 화면 안에 표시되도록 보정), 도서 전환/모드 전환 시 상태 초기화 처리. Playwright로 페이지 전환·경계값(첫/마지막 페이지 버튼 비활성화)·모드 복귀 시 스타일 초기화까지 검증 완료. |
| **이용 가이드 · 1:1 문의 · 이용약관 · 개인정보처리방침 일본어 번역 누락** | 언어를 日本語로 전환해도 상단 내비게이션 등 UI 라벨만 바뀌고, 4개 정보 페이지의 실제 본문 내용은 한국어 그대로 노출됨 | 4개 페이지(`templates/pages/guide_and_info.html`) 본문이 `data-i18n` 바인딩 없이 순수 정적 한국어 HTML로만 작성되어 있어, 기존 `I18N_DICT`/`setLanguage()` 번역 엔진의 적용 대상에서 완전히 누락되어 있었음 | 1. 4개 페이지의 제목 및 섹션(총 22개 콘텐츠 블록 + 공용 "← 돌아가기" 버튼)에 `data-i18n` 키를 부여.<br>2. `I18N_DICT`의 `ko`/`ja` 양쪽에 대응 키를 추가해 `ko` 값은 기존 원문과 100% 동일하게, `ja` 값은 서비스 안내문·이용약관·개인정보처리방침에 맞는 자연스러운 정중체(です/ます) 일본어로 신규 번역 (예: 가공독서회 → 기존 `lib_profile_since` 번역 관례를 따라 "架空読書会"로 통일, 독서방→読書室, 사회자→司会者).<br>3. Playwright로 4개 페이지 전체를 日本語 전환 후 실제 렌더링된 텍스트를 직접 추출해 번역 적용 여부를 검증하고, 한국어로 재전환 시 원문(이스케이프된 큰따옴표 포함)이 손상 없이 복원되는지까지 확인 완료. |

---

## 📅 2026년 8월 18일 (저장형 XSS·관리자 계정·DB 자격증명 로그 노출 보안 긴급 패치, 반응 기능 서버 연동 복구, 영구 활성 샘플 독서방 복구 & AI 표지 생성 파이프라인 정밀 점검)

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **보안 취약점 (저장형 XSS)** | 실시간 채팅방 및 아카이브(독서방 종료 후 매거진 뷰)에서 채팅 내용과 닉네임이 이스케이프 없이 노출되어, 악성 스크립트가 담긴 채팅 메시지 하나로 `localStorage`에 저장된 다른 로그인 사용자의 세션 토큰을 탈취할 수 있는 취약점 존재 (140자 채팅 제한 내에서도 충분히 실행 가능) | `books.js`의 `buildMsgEl()`(실시간 채팅 발신자 닉네임·아바타), `openArchive()`(아카이브 매거진 뷰의 채팅/댓글 스레드), `buildReadOnlyMsgEl()`(채팅 기록 열람 아바타)에서 문자열을 `.innerHTML`에 직접 삽입하면서 기존에 존재하던 `escHtml()` 이스케이프 함수 적용이 다수 지점에서 누락됨. 추가로 백엔드 `UserSignup.nickname` 필드에 형식 검증이 전혀 없어, 프론트엔드의 "자동 배정" UI를 우회해 API를 직접 호출하면 임의의 HTML/스크립트를 닉네임으로 그대로 저장할 수 있었음 | 1. `books.js`의 미이스케이프 지점(발신자 닉네임, 아바타 이니셜, 채팅 본문, 아카이브 댓글·채팅 스레드 전체) 전수에 `escHtml()` 적용.<br>2. `main.py`의 `UserSignup.nickname`에 `Field(pattern=r'^[가-힣a-zA-Z0-9]+$', max_length=20)` 서버측 검증을 신규 추가하여 악성 닉네임 저장 자체를 원천 차단. |
| **기능 버그 (데이터 미영속)** | 채팅 메시지에 이모지 반응(❤️🤔😄✨)을 남겨도 새로고침하면 사라지고, 다른 독자에게는 전혀 보이지 않음. 백엔드에는 반응 저장 API(`POST /api/chats/{id}/react`)와 DB 컬럼(`reactions`)이 이미 갖춰져 있었지만 실질적으로 호출되지 않는 죽은 코드 상태였음 | 프론트엔드 `toggleChatRx()`가 로컬 메모리 상의 `msg.reactions` 객체만 변경하고 화면을 재렌더링할 뿐, 실제 백엔드 API를 단 한 번도 호출하지 않았음 | 1. `toggleChatRx()`가 반응 추가/취소 시 실제로 `POST`/`DELETE /api/chats/{id}/react`를 호출하도록 연결 (비로그인 시 안내 토스트, 요청 실패 시 로컬 상태 자동 롤백).<br>2. 기존엔 반응 증가만 가능했던 것을 보완해, `main.py`에 반응 취소(감소)용 `DELETE /api/chats/{chat_id}/react` 엔드포인트 신규 추가. |
| **보안 취약점 (하드코딩된 약한 자격증명)** | AI 사회자 겸 관리자 계정(`admin@admin.com`)의 비밀번호(`admin1234`)가 소스코드에 그대로 하드코딩되어 있어, 이 값을 아는(또는 추측한) 누구나 정상 로그인 절차로 로그인해 도서를 영구 삭제(관리자 권한)하거나 "🎙️ AI 사회자" 공식 계정으로 채팅을 작성(공식 계정 사칭)할 수 있는 상태였음 | `get_or_create_moderator()`가 환경변수 없이 고정 문자열(`"admin1234"`)로 관리자 계정을 시딩 | 기존 `JWT_SECRET_KEY` 처리 방식과 동일한 패턴으로, `.env`의 `ADMIN_SEED_PASSWORD`를 읽어 관리자 계정 비밀번호로 사용하도록 개선하고, 미설정 시 콘솔에 명확한 보안 경고를 출력하도록 조치. (단, 이미 `admin@admin.com` 계정이 생성되어 있는 기존 DB에는 소급 적용되지 않으므로, 운영 중인 DB가 있다면 해당 계정 비밀번호를 별도로 변경 권장) |
| **다국어(i18n) 번역 누락** | 언어를 일본어(日本語)로 전환해도 상단 로고와 하단 푸터 저작권 문구만 한국어로 그대로 남아있음 | `nav.html`/`footer.html`에 `data-i18n="logo_text"`, `data-i18n="footer_copy"` 속성은 부여되어 있었지만, 정작 번역 사전 `I18N_DICT`에는 두 키가 등록되어 있지 않아 `setLanguage()`가 조용히 건너뜀 | `books.js`의 `I18N_DICT` `ko`/`ja` 양쪽 사전에 `logo_text`, `footer_copy` 키를 신규 등록. |
| **보안 취약점 (평문 자격증명 로그 노출)** | AI 표지 생성 기능 라이브 점검 중 발견. 서버가 시작될 때마다 콘솔/로그에 `[DB] 연결 시도: mysql+pymysql://root:<실제비밀번호>@***` 형태로 **MySQL 비밀번호가 마스킹되지 않고 평문 그대로 출력**됨 | `database.py`의 마스킹 로직 `DATABASE_URL[:DATABASE_URL.index('@')+1]`이 URL에서 `@` **앞부분**(계정+비밀번호)을 그대로 남기고 `@` **뒷부분**(호스트)만 `***`로 가리는, 의도(비밀번호 은닉)와 정반대의 로직으로 작성되어 있었음 | `database.py`에서 `urlparse()`로 DATABASE_URL을 분해해 비밀번호만 `***`로 치환하고 scheme/username/host/port/db명은 그대로 출력하도록 마스킹 로직을 정정 (`mysql+pymysql://root:***@localhost:3306/gakong_db` 형태로 정상 출력). 단, 이 세션 중 실제 비밀번호가 터미널에 이미 노출되었으므로 해당 MySQL 계정 비밀번호는 별도로 변경 권장. |
| **기능 버그 (영구 활성 샘플 독서방 자동 아카이브)** | 개발 서버를 열어보니 홈 화면에 활성 독서방이 0개로 노출됨(데이터 유실로 오인하기 쉬운 증상). 원래 상시 활성화(`deadline_days=9999, is_archived=False`)로 설계된 영구 샘플 독서방 『깨진 렌즈가 비춘 잃어버린 항해』조차 아카이브되어 있었음 | `seed_lost_voyage_book_if_needed()`에 `deadline_days=9999`/`is_archived=False` 상시 활성화 로직이 추가되기 **이전**에 이미 생성되어 있던 구버전 DB 레코드(`deadline_days=10`)라서 일반 10일 만료 규칙에 걸려 자동 아카이브됐고, 이 함수가 "이미 존재하면 건너뜀" 방식이라 이후 서버를 아무리 재시작해도 한 번도 복구되지 않았음. (다른 10권은 정상적으로 10일 만료된 것으로, 버그 아님) | `seed_lost_voyage_book_if_needed()`에 `elif book.deadline_days != 9999 or book.is_archived:` 보정 분기를 추가하여, 기존 레코드가 의도한 영구 활성 상태와 어긋나 있으면 서버 시작 시마다 자동으로 복구하도록 개선. |
| **AI 표지 견고성 (에러 응답을 이미지로 오인)** | AI 표지 생성 파이프라인 정밀 점검 중 발견. Pollinations AI 호출 시 `status_code==200 and size>5000`만으로 성공 여부를 판단하여, 만약 5KB를 넘는 에러/HTML 페이지가 반환되면 그대로 `.png`로 저장해 깨진 표지가 노출될 여지가 있었음 | `generate_book_cover_art()`가 응답 바이트를 실제로 디코딩 가능한 이미지인지 검증하지 않고 상태코드·용량만으로 저장을 결정 | 저장 직전 `PIL.Image.open(io.BytesIO(res.content)).verify()`로 실제 이미지 디코딩 가능 여부를 검증한 뒤에만 파일로 저장하도록 강화. 검증 실패 시 다음 시도로 자연스럽게 넘어가고, 실패/비정상 응답(상태코드·용량) 로그도 함께 추가해 원인 추적이 쉽도록 개선. |
| **AI 표지 견고성 (PIL 폴백 전용 렌더러가 신규 도서에 미적용)** | Editorial PIL 폴백에 9종의 전용 사물 벡터 렌더러가 있었지만 전부 기존 시드 도서 9권의 **정확한 제목 문자열**에만 매칭되도록 짜여 있어, 매일 Gemini가 새로 만드는 도서는 거의 항상 마지막 범용 랜덤 도형(오브/크리스탈/모래시계 등) 분기로 빠짐 | `_generate_editorial_cover_pil()`의 분기 조건이 `"오르골" in title` 처럼 제목 완전 일치만 검사하고, `_extract_central_motif()`가 이미 추출해 둔 모티프 키워드는 전혀 재사용하지 않았음 | `_extract_central_motif()`가 프롬프트 문자열과 함께 `motif_key`(예: `music_box`, `typewriter`, `camera` 등)도 반환하도록 개선하고, `_generate_editorial_cover_pil()`의 8개 분기 조건에 `motif_key` 일치 여부를 함께 검사하도록 연결. 이제 제목이 기존 시드 도서와 달라도 줄거리에 같은 소재(오르골, 타자기, 카메라 등)가 등장하면 해당 전용 렌더러가 적용됨. (라이브 테스트로 신규 제목 + "오르골" 줄거리 조합이 `music_box` 렌더러로 정확히 매칭되는 것을 확인) |
| **AI 표지 견고성 (프로세스 재시작마다 달라지는 시드)** | Pollinations 요청 시드를 파이썬 내장 `hash()`로 계산하고 있었는데, `hash()`는 `PYTHONHASHSEED` 프로세스별 랜덤 솔트의 영향을 받아 서버를 재시작하면 동일 도서라도 다른 시드가 나옴. "도서 고유 시드"라는 설계 의도와 실제 동작이 어긋나 있었음 (표지가 파일로 캐시되어 재사용되므로 실사용 피해는 낮았음) | `seed = abs(hash(title + unique_id + attempt)) % 999999 + 1000`가 프로세스 간 비결정적인 내장 `hash()`에 의존 | `hashlib.md5(...)` 기반의 결정적 해시로 시드 계산 로직을 교체하여, 동일한 (제목, unique_id, attempt) 입력이면 서버를 몇 번을 재시작해도 항상 동일한 시드가 나오도록 수정. |
| **죽은 코드 정리** | 표지 이미지 위에 한글 제목·작가명·장르 뱃지를 직접 합성해 넣는 `_add_text_to_cover_pil()`(약 115줄)이 실제로는 어디에서도 호출되지 않는 죽은 코드로 남아있었음 | 과거 "이미지에 텍스트 오버레이" 방식에서 "이미지는 텍스트 없는 순수 컨셉아트로 유지"(8월 3일 개편) 방식으로 설계가 바뀌면서 호출부만 제거되고 함수 정의는 정리되지 않고 남음 | `main.py`에서 `_add_text_to_cover_pil()` 함수 전체를 삭제. |
| **표지 파일 정리 로직 부재** | 관리자가 도서를 삭제해도 `static/covers/`에 있는 실제 표지 PNG 파일은 지워지지 않고 디스크에 계속 쌓이는 상태였음 | `DELETE /api/books/{book_id}` 핸들러가 DB 레코드만 삭제(`db.delete(book)`)하고 연결된 표지 파일은 건드리지 않음 | `_delete_cover_file_if_exists()` 헬퍼를 신규 추가하고, 도서 삭제 시 DB 레코드 삭제 직전에 호출하여 더 이상 참조되지 않는 표지 파일을 함께 정리하도록 개선. |
| **신규 기능: 활성 독서방 자동 보충** | (버그 아님, 개선) 새 독서방은 유저가 로그인해서 "새 책 생성"을 눌러야만(1일 1회 제한) 만들어지고 10일 후 자동 만료되는 구조라, 한동안 방문자가 뜸하면 활성 독서방이 0권까지 떨어져 홈 화면이 완전히 텅 비는 구조적 리스크가 있었음 (실제로 라이브 점검 중 활성 도서 0권 상태를 목격함) | 콘텐츠 공급이 전적으로 사람의 능동적인 행동에만 의존하고, 공급이 끊겼을 때 시스템이 스스로 채워주는 안전장치가 없었음 | 기존 "새 책 생성" 엔드포인트의 핵심 로직(pool 우선 재사용 → 부족분만 Gemini 생성)을 `_produce_candidate_books()` 공용 함수로 분리하고, 1시간마다 활성 독서방 수를 확인해 **0권일 때만** 사람의 선택 없이 3권을 즉시 자동 채택(활성화)하는 `auto_refill_loop()` 백그라운드 루프를 신규 추가. 1권이라도 있으면 아무 것도 하지 않아 무분별한 자동 생성을 방지. (라이브 테스트로 0권 상황 재현 후 3권이 pool에서 무료로 재사용되어 즉시 채워지는 것을 확인 완료) |

### 🔍 코드리뷰 · 보안점검 결과 (수정하지 않고 보고만 완료한 항목)
사용자 요청으로 전체 코드베이스(`main.py`, `auth.py`, `models.py`, `database.py`, `books.js`, `auth.js`, 템플릿 전체)를 대상으로 코드리뷰 및 보안점검을 함께 수행했습니다. 위 4건 외에 아래 항목은 심각도가 낮거나 트레이드오프가 있어 조치 여부를 사용자 판단에 맡기고 보고만 완료했습니다.
- **이메일 열거(Enumeration) 가능성**: `/api/auth/find-password`가 가입 여부에 따라 다른 에러 메시지를 반환해 가입 이메일 추정이 가능함 (표준적인 트레이드오프).
- **인증 없는 관리용 API**: `POST /api/books/auto-archive`가 인증 없이 호출 가능하나, 60초 주기 백그라운드 루프와 동일 동작이라 실질 위험은 낮음.
- **`_login_fail_attempts` 인메모리 무제한 증가**: 로그인 실패 기록 dict가 영구 메모리에 쌓여 장시간 운영 시 조금씩 증가 (멀티 워커 배포 시 워커별로 별도 작동).
- **`toggleChatSearch()` 데드 코드**: 채팅 검색창 UI가 상시 노출 방식으로 개편되며 토글 버튼 자체가 템플릿에서 사라졌으나, 호출부 없는 미사용 함수가 잔존.

---

## 📅 2026년 8월 24일 (인증·인가 계층 재구성, 보안 구조 개선, 모바일 UI 대응, 일본어 i18n 완성 & 아카이브 월별 서가)

> 사용자 요청("보안 개선할 점 찾아줘" → "유지보수하기 쉬운 쪽으로 바꿔줘")에 따라 전체 코드베이스 보안 점검을 수행하고, 발견된 취약점을 **개별 패치가 아닌 구조 개선 방식**으로 수정했습니다. 설정은 `config.py`, 요청 단위 보안 장치는 `security.py`, 인증·인가는 `auth.py`로 각각 일원화하여 앞으로 같은 유형의 누락이 반복되지 않도록 했습니다.

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **보안 구조 개선 (설정·시크릿 관리 일원화)** | 환경변수를 읽는 코드가 `main.py`, `auth.py`, `database.py` 세 곳에 흩어져 있고 필수 시크릿이 없을 때의 처리 방식도 파일마다 달랐음(대부분 경고만 출력한 뒤 하드코딩 값으로 기동). 특히 `main.py`는 `.env`를 직접 한 줄씩 파싱해 `os.environ`에 넣고 있어 따옴표·공백 처리가 없고 예외를 통째로 무시했음 | 설정 진입점이 단일화되어 있지 않아, 새 시크릿을 추가할 때마다 "없으면 어떻게 할지"를 매번 다르게 구현하게 되는 구조적 문제 | 설정 단일 진입점 **`config.py` 신규 추가**. `.env`는 `dotenv`로 한 번만 로드하고, `require_secret()` 헬퍼가 `APP_ENV=production`에서 필수 시크릿 누락 시 **`ConfigError`로 기동을 중단**(개발에서는 경고 후 임시값 사용)하도록 통일. `main.py`의 수동 `.env` 파싱 블록을 제거하고, 커밋 가능한 설정 템플릿 **`.env.example`**을 추가. |
| **보안 취약점 (인증 없는 비밀번호 재설정 — 계정 탈취/영구 잠금)** | `POST /api/auth/find-password`가 인증·레이트리밋 없이 **이메일 주소만 알면 피해자의 비밀번호를 즉시 임시값으로 교체**했음. 공격자가 남의 이메일로 호출하기만 하면 피해자는 자기 비밀번호로 더 이상 로그인할 수 없게 되고(영구 잠금 DoS), 메일 계정이 함께 노출된 경우 그대로 계정 탈취로 이어짐. 미가입 이메일에는 400 + "등록되지 않은 이메일"을 반환해 **가입자 이메일 열거**(8월 18일 점검 시 보고만 했던 항목)도 가능했음 | "임시 비밀번호를 즉시 발급하고 메일로 알려준다"는 흐름 자체가, 요청자가 계정 소유자인지 확인하지 않은 상태에서 비밀번호를 변경해버리는 구조였음 | **재설정 토큰 링크 방식으로 전면 교체.** 1. 요청은 더 이상 비밀번호를 변경하지 않고, `secrets.token_urlsafe(32)` 1회용 토큰을 발급해 메일로 링크만 발송 (**DB에는 원문이 아닌 SHA-256 해시만 저장**, 30분 후 만료).<br>2. `POST /api/auth/reset-password` 엔드포인트와 재설정 페이지 `templates/reset_password.html`(`GET /reset-password`) 신규 추가. 토큰은 사용 즉시 폐기.<br>3. 가입 여부와 무관하게 **항상 동일한 응답**을 반환해 이메일 열거 차단.<br>4. IP·이메일 두 축으로 시간당 3회 레이트리밋 적용.<br>(라이브 검증: 유효 토큰 200 → 새 비밀번호 로그인 성공, 옛 비밀번호 401, 동일 토큰 재사용 400, 요청만으로는 비밀번호 불변 확인) |
| **보안 취약점 (JWT 시크릿 하드코딩 폴백)** | `JWT_SECRET_KEY`가 `.env`에 없으면 소스코드에 박힌 고정 문자열로 **경고만 출력한 뒤 그대로 기동**했음. 이 상태로 배포되면 소스를 본 누구나 임의의 `user_id`를 담은 토큰을 위조해 타인(관리자 포함) 계정으로 로그인할 수 있었음 | "없으면 기본값으로라도 돌아가게" 하는 개발 편의 처리가 운영 환경과 구분되지 않은 채 남아 있었음 | `config.require_secret()`으로 통일해 운영(`APP_ENV=production`)에서는 미설정 시 **기동 자체가 실패**하도록 변경. 관리자 시드 비밀번호(8월 18일 패치에서 `.env`화했으나 미설정 시 `admin1234`로 폴백하던 문제)도 동일하게 처리하고, 하드코딩된 관리자 이메일은 `MODERATOR_EMAIL` 상수로 통일. |
| **보안 취약점 (저장형 XSS 잔존 지점 · 속성값 탈출)** | 8월 18일 패치에서 채팅·아카이브 경로는 이스케이프했지만 **도서 제목·줄거리·저자·장르·가격, 감상 랭킹 닉네임, AI 작가 한줄평, 후보 도서 카드, 책등(서재/아카이브), 채팅 타임스탬프** 등 25개 삽입 지점이 여전히 이스케이프 없이 `innerHTML`에 들어가고 있었음. 또한 `escHtml()`이 앰퍼샌드와 부등호만 처리해 값이 속성값 자리(style 속성 등)에 들어가면 **따옴표로 속성을 탈출**할 수 있었고, `getCoverCss()`는 표지 URL을 CSS url() 안에 그대로 문자열 결합했음 | 이스케이프 적용이 "발견한 지점만 개별 수정" 방식이라 누락이 반복되는 구조. 헬퍼 자체도 속성값 문맥을 고려하지 않았고, CSS를 만드는 함수는 호출부 10곳이 각자 알아서 처리해야 했음 | 1. `escHtml()` 강화 — 큰따옴표·작은따옴표까지 이스케이프, null·숫자 등 비문자열 안전 처리, `window.escHtml`로 노출. 누락된 25개 지점 전수 적용.<br>2. **`getCoverCss()` 내부에 `safeCssUrl()`**(따옴표·괄호·세미콜론 제거)**과 `safeCssColor()`**(CSS 색상 리터럴 문자만 허용, 그 외 기본색 대체)**를 도입**해 호출부 10곳을 한 번에 보호.<br>3. 브라우저 라이브 검증: img·script 태그 페이로드 주입 시 생성된 태그 0개·텍스트로만 렌더, 표지 CSS 공격 문자열 무력화 확인. |
| **보안 취약점 (채팅 검색 하이라이트의 마크업 파괴)** | `onChatSearch()`가 채팅 버블의 `innerHTML` **문자열을 정규식으로 치환**해 mark 태그를 삽입했음. 검색어가 태그명·클래스명·속성값과 겹치면 마크업이 깨지거나 의도치 않은 태그가 만들어질 수 있었음 | 하이라이트를 DOM이 아니라 HTML 문자열 수준에서 처리했기 때문 | 텍스트 노드만 순회해 mark 요소를 DOM API로 감싸는 `highlightTextNodes()` 헬퍼로 교체 (`textContent` 사용, 빈 매치 무한루프 방지 포함). 검색어가 태그명과 겹쳐도 마크업이 보존되는 것을 브라우저에서 검증. |
| **보안 취약점 (토큰 무효화 수단 부재)** | 비밀번호를 변경하거나 회원을 탈퇴해도 이미 발급된 JWT는 만료(기본 24시간)까지 계속 유효했음. 토큰이 탈취된 경우 비밀번호를 바꿔도 공격자의 세션을 끊을 방법이 없었고, 탈퇴한 계정의 토큰도 통과했음 | 토큰이 완전 무상태(stateless)로 검증되어, 서버가 "이 토큰은 더 이상 유효하지 않다"고 판단할 근거가 전혀 없었음 | 1. `users.password_changed_at` 컬럼을 신규 추가하고 토큰 발급 시 `iat`(발급 시각)를 함께 넣어 **발급 시각이 비밀번호 변경 시각보다 이르면 거부**.<br>2. 비밀번호 변경 경로를 `auth.set_password()` 한 곳으로 모아 해시 교체와 토큰 무효화가 항상 함께 일어나도록 통일.<br>3. 토큰 검증 시 사용자 레코드를 조회하므로 **탈퇴한 계정의 토큰도 즉시 거부**.<br>(라이브 검증: 비밀번호 변경 후 기존 토큰으로 `/api/auth/me` 호출 시 401) |
| **보안 취약점 (무차별 대입 방어 로직의 허점)** | 로그인 실패 기록이 **이메일 키 기반 프로세스 메모리 dict**(`_login_fail_attempts`)여서 세 가지 문제가 있었음. ① 공격자가 남의 이메일로 5회 실패시키면 **피해자를 15분간 로그인 불가 상태로 만들 수 있음(DoS)**, ② 워커 다중화·서버 재시작 시 초기화, ③ IP 기준 제한이 없어 이메일을 바꿔가며 하는 password spraying은 무제한. 회원가입·비밀번호 찾기에는 제한이 아예 없어 대량 계정 생성·메일 발송이 가능했음 | 엔드포인트마다 임시 dict로 시도 횟수를 세는 방식이라 정책이 한곳에 모여 있지 않았고, 제한 키를 이메일만으로 잡았음 | 슬라이딩 윈도우 제한기 **`security.RateLimiter`를 신규 추가**하고 용도별 인스턴스로 정책을 한눈에 모았음. 로그인은 **IP(10회/15분) + 계정(20회/15분) 두 축**으로 분리해 분산 공격은 막으면서 피해자 계정 잠금 DoS는 어렵게 하고, 가입(5회/시간)·비밀번호 재설정(3회/시간)에도 적용. 저장소만 교체하면 Redis 등 공유 저장소로 확장할 수 있도록 분리. |
| **보안 취약점 (인가 검사 중복 · 인증 없는 상태 변경 API)** | 관리자 검사가 엔드포인트마다 사용자 조회 + `is_admin` 확인 + 403 형태로 복사돼 있었고, `POST /api/books/auto-archive`는 **인증 없이 누구나 호출해 독서방을 아카이브 처리**할 수 있었음 (8월 18일 점검 시 보고만 했던 항목) | 인가 로직이 의존성으로 추출되어 있지 않아 새 관리자 API를 추가할 때마다 검사를 복사해야 했고, 그 과정에서 누락이 발생 | `auth.require_admin` / `auth.get_current_user` 의존성을 신규 추가해 인가를 한곳으로 모으고 `delete_book`의 중복 검사를 제거. `auto-archive`도 관리자 전용으로 전환.<br>(라이브 검증: 일반 유저 403, 비로그인 401) |
| **보안 헤더 · CORS · API 문서 노출** | CSP, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy` 등 보안 응답 헤더가 전무했고, `ALLOW_ORIGINS` 미설정 시 **모든 출처(와일드카드)를 허용**했음. 운영에서도 `/docs`, `/openapi.json`이 그대로 열려 있었음 | 브라우저 측 2차 방어선이 없어 XSS가 한 번 뚫리면 피해를 제한할 수단이 없었고, CORS 기본값이 "전부 허용"이었음 | 1. `security.SecurityHeadersMiddleware`를 추가해 CSP·nosniff·`X-Frame-Options: DENY`·Referrer-Policy·Permissions-Policy를 모든 응답에 부착(운영에서는 HSTS 추가).<br>2. CORS 와일드카드를 **`ConfigError`로 금지**하고 `ALLOW_ORIGINS` 명시 목록만 허용. 개발 환경에서는 포트를 바꿔 띄울 수 있도록 `allow_origin_regex`로 localhost의 모든 포트만 자동 허용.<br>3. 운영에서 `docs_url`·`redoc_url`·`openapi_url` 비활성화.<br>(라이브 검증: 외부 출처 차단, localhost 다른 포트 허용) |
| **민감정보 로그 노출 (임시 비밀번호 · SMTP 예외)** | SMTP가 플레이스홀더 상태일 때 발급된 임시 비밀번호를 **콘솔에 평문으로 출력**했고, SMTP 발송 실패 시 예외 메시지를 그대로 출력해 자격증명 관련 정보가 로그에 섞일 여지가 있었음 | 개발 편의를 위한 "모의 발송" 출력이 운영 환경과 구분 없이 동작 | 메일 발송을 `_send_email()` 하나로 통합하고, 실패 시 **예외 유형(클래스명)만** 로그에 남기도록 변경. 재설정 링크는 **개발 환경에서만** 콘솔에 출력하고 운영에서는 절대 출력하지 않음. |
| **유지보수성 개선 (DB 스키마 패치 블록 정리)** | (버그 아님, 개선) 컬럼 추가용 try/except 블록이 컬럼마다 복사되어 8개까지 늘어나 있었고, 실패 시 rollback 없이 넘어갔음 | 컬럼을 하나 추가할 때마다 5~6줄의 동일한 블록을 복사해야 하는 구조 | `_SCHEMA_PATCHES` 목록(테이블, 컬럼, 타입)과 단일 루프로 재작성. 이제 컬럼 추가는 **목록에 한 줄 추가**로 끝나며, 실패 시 rollback도 함께 처리. 이번에 추가된 `password_changed_at`, `reset_token_hash`, `reset_token_expires_at` 3개 컬럼도 이 목록으로 관리. |
| **모바일 UI 미대응 (반응형 레이아웃 부재)** | 5,213줄 분량의 `styles.css`에 미디어쿼리가 **단 한 개도 없어** 데스크톱 고정 레이아웃만 존재했음. 375px 화면에서 실측한 결과 ① 3열 고정 카드 그리드 때문에 **책 카드 폭이 78px까지 찌그러지고**, ② 내비게이션 메뉴가 3줄로 접히며 높이 99px로 넘쳐 헤더(68px) 밖으로 삐져나오고, ③ 좌우 28~48px 고정 여백이 좁은 화면을 크게 잠식했음 | 화면 폭에 따른 분기 없이 데스크톱 기준 고정값(3열 그리드, 28~48px 여백, 340~420px 고정 너비 모달)으로만 작성되어 있었음 | **모바일 전용 레이어 `static/css/mobile.css` 신규 추가.** `styles.css` 다음에 로드되어 덮어쓰기만 하므로 데스크톱 레이아웃은 그대로 유지된다. 브레이크포인트는 태블릿(900px)·모바일(640px)·소형(340px) 3단계로 고정하고, 화면(홈/상세/채팅/후보/서재/아카이브/안내/인증) 단위로 구획을 나눠 새 규칙을 넣을 자리를 명확히 했다.<br>주요 조정: 카드 그리드 3열 → 2열(소형 1열), 내비게이션 한 줄 가로 스와이프, 좌우 여백 28~48px → 16px, 도서 상세 표지+정보 2단 → 1단, 평점 통계 2단 → 1단, 후보 도서 3장 나열 → 스와이프 스냅, 아카이브 참여자 5열 → 2열, 고정 너비 모달·표지 확대 뷰어 → `min(vw, 원래값)`.<br>(라이브 검증: 375px에서 홈·상세·후보·서재·안내·약관·개인정보·문의·로그인·회원가입·아카이브 11개 화면 모두 가로 스크롤 0) |
| **모바일 채팅 헤더 붕괴 · iOS 입력창 자동 확대** | 375px에서 독서방 채팅 헤더가 **121px까지 부풀었음**(데스크톱 65px). 제목이 3줄로 접히고 280px 고정 검색창이 폭을 대부분 차지한 탓. 또한 입력창 글자 크기가 16px 미만이라 iOS 사파리에서 입력창을 탭할 때마다 화면이 자동 확대됨 | 헤더가 `제목(flex:1) + 고정폭 280px 검색창` 구조여서 좁은 화면에서는 제목에 남는 폭이 99px뿐이었음. 검색창 확장도 `:hover` 기준이라 hover가 없는 터치 환경에서는 동작 방식이 맞지 않았음 | 제목을 한 줄 말줄임(`text-overflow: ellipsis`)으로 고정하고, 검색창은 평소 아이콘 크기(42px)로 접어 두었다가 **탭해서 포커스되면 펼쳐지도록**(`:focus-within`) 변경. 터치 환경에 맞지 않는 `:hover` 확장 규칙은 모바일에서 무효화. 입력 요소(`input`, `textarea`)는 모바일에서 16px 이상을 유지하도록 지정해 자동 확대를 차단.<br>(라이브 검증: 헤더 121px → 65px, 검색창 42px ↔ 248px 정상 토글) |
| **모바일 레이아웃 작업 중 자체 발견한 회귀** | 모바일 대응 과정에서 넣은 `overflow-wrap: anywhere` 규칙이 언어 전환 버튼("한국어")을 **한 글자씩 줄바꿈**시켜 내비게이션 높이를 오히려 더 키웠음 | 좁은 flex 항목 안에서는 `overflow-wrap: anywhere`가 단어 중간을 강제로 끊기 때문 | 해당 규칙을 제거하고 `word-break: keep-all`(어절 단위 줄바꿈)만 남긴 뒤, 내비게이션 항목에는 `white-space: nowrap`을 지정해 한 줄을 유지하도록 수정. mobile.css 주석에 재발 방지용으로 이유를 남겨 두었다. |
| **모바일 실기기 화면 확인 후 추가 수정 (사용자 스크린샷 피드백)** | iPhone 15 Pro Max(430px) 실화면에서 ① "새 책 소환하기" 배너의 설명 문구가 두세 단어씩 끊겨 세로로 길게 늘어지고 **생성 버튼이 카드 밖으로 71px 삐져나감**, ② 내비게이션 오른쪽 "일본어"가 잘려 보임, ③ 도서 상세 평점 통계의 세로 구분선이 1단 배치에서 부모 밖으로 넘침 | ① 앞선 작업에서 `.gen-btn`에 넣은 `width: 100%`가 **가로 배치(`flex-direction: row`)를 그대로 둔 채** 적용되어, 버튼이 컨테이너 폭 전체를 요구하며 밀려남. ② 내비게이션 항목 합계 폭(383px)이 가용 폭(305px)을 초과. ③ 구분선의 데스크톱 좌우 여백(`margin: 0 20px`)이 `width: 100%`와 합쳐져 40px 초과 | ① `.gen-section`을 세로 배치로 전환하고(설명 문구 위, 버튼 아래 전체 폭) 여백·글자 크기를 모바일에 맞게 조정.<br>② 내비게이션 글자·여백을 압축(합계 383px → 320px)해 **430px 화면에 전부 들어오도록** 하고, 그보다 좁은 화면을 위해 **로그인/프로필 영역을 `position: sticky`로 오른쪽 끝에 고정**해 메뉴가 스크롤돼도 로그인 버튼은 항상 보이게 함. 언어 스위치의 '/' 구분자는 모바일에서 숨김.<br>③ 구분선의 좌우 여백을 해제.<br>(검증 방법 보강: 기존에는 "뷰포트보다 넓은 요소"만 검사해 ①·③처럼 **부모 밖으로 넘치는** 경우를 놓쳤음 → 부모 경계 초과까지 함께 검사하도록 점검 스크립트를 고쳐 393px·430px에서 홈·상세·후보·서재·안내·로그인 전 화면 이상 0건 확인) |
| **일본어 미번역 UI 전수 정리 (i18n 2차)** | 언어를 日本語로 전환해도 로그인·회원가입·내 서재 게이트 화면 전체, 도서 상세의 섹션 라벨(평균 평점·내 평점·채팅 반응·추천사·출판사 서평·상세 목차), 홈의 "전체 N개 / 👥 N명 / 🗃 아카이브 / 곧 마감", 반응 선택 팝오버(공감해요·생각이 달라요·재밌어요·인상 깊어요·답장하기), 각종 토스트·확인창(약 140건), 입력창 placeholder와 title 툴팁이 한국어로 남아 있었음. 또한 **새로고침하면 정적 텍스트만 한국어로 되돌아가는** 문제가 있었음 | ① 사전(`I18N_DICT`)에 이미 있는데 코드에서 부르지 않는 키가 26개 있었고, ② JS가 만들어내는 문자열은 대부분 하드코딩이었으며, ③ `setLanguage()`가 텍스트 노드만 바꿔 `placeholder`/`title` 속성은 손대지 않았고, ④ 페이지 로드 시 `setLanguage()`를 한 번도 호출하지 않아 저장된 언어가 정적 텍스트에 반영되지 않았음 | 1. 문구 조회 헬퍼 **`t(key, fallback)`**를 도입하고 화면에 나가는 모든 문자열이 이 함수를 거치도록 정리(books.js 약 90건, auth.js 약 21건 치환). auth.js는 사전을 따로 두지 않고 `window.t`를 통해 같은 사전을 쓴다.<br>2. `setLanguage()`가 **`data-i18n-placeholder` / `data-i18n-title`** 속성도 번역하도록 확장하고, 이미 열려 있는 상세·채팅 화면과 로그인 영역을 다시 그리도록 보완.<br>3. 템플릿 라벨 39곳에 `data-i18n` 계열 속성 부여.<br>4. 사전에 한국어/일본어 키 **172쌍**을 추가(총 262쌍, 양쪽 개수 일치 검증).<br>5. 페이지 로드 시 저장된 언어를 즉시 적용하도록 초기화 코드 추가.<br>(검증: 일본어로 전환한 실제 화면에서 한국어 텍스트 노드를 전수 추출 → 홈·상세·로그인·회원가입·서재 게이트에서 UI 잔여 0건. 남은 한국어는 DB의 책 데이터뿐) |
| **일본어 글꼴 깨짐 (두부 현상) 대비** | CSS 폰트 스택은 `Noto Sans KR` / `Noto Serif KR`을 지정하고 있었지만 **해당 폰트를 어디서도 로드하지 않아** 실제로는 시스템 폰트로 대체되고 있었음. 일본어 가나·한자가 없는 환경에서는 네모(두부)로 깨질 수 있었고, 한글 폰트가 먼저 적용되면 일본어 한자가 한국식 자형으로 보이는 문제도 있었음 | 웹폰트 링크에 본문용 한글/일본어 폰트가 빠져 있었고, 언어와 무관하게 하나의 폰트 스택만 사용했음 | Google Fonts에서 **Noto Sans KR / Noto Serif KR / Noto Sans JP / Noto Serif JP**(SIL Open Font License, 무료)를 로드하고, `:root`에는 한국어 스택을, `html[lang="ja"]`에는 일본어 스택을 정의해 언어에 맞는 자형이 우선되도록 분리. `setLanguage()`가 `<html lang>`을 갱신하므로 언어 전환·새로고침 모두에서 폰트가 따라온다. (CSP의 style-src·font-src에 Google Fonts가 이미 허용되어 있어 추가 조치 불필요) |
| **신규 기능: 아카이브 월별 서가(月別アーカイブ)** | (버그 아님, 요청 기능) 종료된 독서방이 한 덩어리 그리드로만 나열되어, 권수가 쌓일수록 "언제 끝난 독서방인지" 알기 어려웠음 | 아카이브 목록에 시간 축이 없었음 | 종료일(`archivedDate`) 기준으로 도서를 달 단위로 묶어 보여주는 **월별 서가**를 신규 구현. 사이트의 종이·에디토리얼 톤에 맞춰 왼쪽에 얇은 서가 기둥(세로선)을 두고, 달마다 앰버색 점 + 세리프 월 라벨 + 권수를 배치했으며, 연도가 바뀌는 지점에만 연도 마커를 크게 노출한다. 월 헤더는 스크롤 시 상단에 고정된다.<br>**규모 대응**(권수가 많아질 때): ① 연도가 2개 이상이면 연도 칩 필터 노출, ② 최신 3개월만 펼치고 이전 달은 접어 두며(접힌 달의 카드는 펼칠 때 생성), ③ 한 번에 12개월씩만 그리고 "이전 아카이브 더 보기"로 이어 붙인다. 정책 수치는 `EXPANDED_MONTHS` / `MONTHS_PER_PAGE` 상수만 바꾸면 조정된다.<br>(검증: 실제 데이터 10권/3개월 정상 그룹화, 합성 데이터 120권/48개월에서도 최초 렌더 1ms·DOM 카드 8장만 생성. 연도 칩·접기·더 보기 동작과 한/일 월 표기(2026년 8월 / 2026年8月) 확인. 모바일 393px에서 2열·헤더 sticky 56px 정상) |
| **보안 취약점 (봇 계정에 운영 권한 · 공식 계정 사칭)** | 서버·DB 전수 점검 중 발견. ① 관리자 계정이 3개였고 그중 **둘이 AI 사회자(봇) 계정**이었음 — 봇 비밀번호가 노출되면 도서 영구 삭제까지 가능한 상태. ② `get_or_create_moderator()`가 서버 기동마다 사회자에게 `is_admin = True`를 **강제로 다시 부여**해, 수동으로 권한을 꺼도 재시작하면 되살아났음. ③ 사회자 계정이 2개(`admin@admin.com` 12건 작성 / `moderator@gakong.com` 0건 유령 계정)로 갈라져 있었음. ④ 프론트가 **닉네임에 '사회자'가 들어있는지**로 사회자를 판별해, 닉네임 규칙(`^[가-힣a-zA-Z0-9]+$`)상 가능한 "사회자"로 가입하면 공식 계정처럼 금색으로 렌더링되고 답장·반응이 차단되는 사칭이 가능했음 | 하나의 `is_admin` 플래그가 "운영 권한"과 "AI 사회자 페르소나"라는 서로 다른 개념을 겸하고 있었고, 프론트는 서버가 알려주지 않는 정보를 닉네임 문자열로 추측하고 있었음 | **역할을 두 축으로 분리.** 1. `users.is_bot` 컬럼 신규 추가 — 봇은 `is_bot=True, is_admin=False`.<br>2. `enforce_roles()`를 신설해 기동 시마다 상태를 보장: 운영자는 `config.OWNER_ADMIN_EMAIL`(기본 `kty98116@naver.com`) **한 명뿐**이며 나머지 계정의 관리자 권한은 회수하고, 중복 사회자 계정은 **메시지를 정식 봇으로 이관한 뒤 삭제**한다.<br>3. **봇 계정 로그인 차단** — 로그인 엔드포인트에서 `is_bot`이면 403.<br>4. 채팅 API 응답에 `isBot` 플래그를 실어 보내고, 프론트는 닉네임 대신 이 값으로 판별하도록 교체(실시간·전송응답·아카이브 보관본 3경로 모두).<br>(적용 결과: 관리자 3명 → **1명**, 사회자 계정 2개 → **1개**, 채팅 43건 전량 보존, 유령 계정 1개 삭제. 검증: 봇 계정 실제 로그인 시도 **403**, 닉네임만 '사회자'인 유저는 일반 독자로 렌더링됨) |
| **서버 초기화가 로그 한 줄 때문에 통째로 중단** | 위 역할 정리 작업이 조용히 실패. 콘솔에는 `Error initializing AI Moderator or Seed data: 'cp949' codec can't encode character '\U0001f399'` 만 남고, 계정 정리·시드 생성이 모두 중단된 채 서버는 정상 기동한 것처럼 보였음 | Windows 콘솔 기본 인코딩(cp949)이 이모지를 표현하지 못하는데, 정리 로그가 `🎙️ AI 사회자` 닉네임을 그대로 출력하려다 `UnicodeEncodeError` 발생. `lifespan`의 포괄적인 `try/except`가 이 예외를 삼켜서, **초기화 실패가 눈에 띄지 않았음** | `main.py` 최상단에서 `sys.stdout`/`sys.stderr`를 `errors="replace"`로 재설정해, 표현할 수 없는 문자는 대체 문자로 출력되고 **로그 때문에 로직이 죽지 않도록** 조치. (인코딩 자체는 바꾸지 않아 기존 한글 로그 가독성은 그대로 유지) |
| **버그 (새 책 후보가 빈 카드로 표시됨)** | "새 책 생성"을 누르면 후보 카드 3장이 제목·표지·줄거리 없이 빈 상태로 뜨고, "이 책으로 독서방 열기" 버튼도 동작하지 않음. API 응답을 직접 확인하니 `[{}, {}, {}]` — 후보 3건이 **전부 빈 객체**로 내려가고 있었음 | `generate_candidates`가 SQLAlchemy ORM 객체를 그대로 반환하는데, 반환 직전 `user.last_generation_at` 갱신을 위해 호출한 `db.commit()`이 **모든 인스턴스를 만료(expire)**시킨다. FastAPI가 응답을 직렬화하는 시점은 그 이후라서 속성이 전부 비어 있는 상태로 인코딩됐다. (하루 1회 제한 기능이 추가되면서 생긴 회귀로, 채택 API는 `db.refresh()`를 하고 있어 멀쩡했기에 더 늦게 발견됨) | 응답용 `serialize_candidate()` 헬퍼를 만들어 **커밋 전에 값을 확보**한 뒤 dict 목록을 반환하도록 변경. ORM 객체를 그대로 응답으로 내보내지 않게 되어 내부 컬럼이 실수로 노출될 여지도 함께 없앴다. |
| **UX (고르지 않으면 후보가 사라지고 하루 동안 갇힘)** | 후보 3권을 받아놓고 고르지 않은 채 화면을 벗어나면 다시 볼 방법이 없었고, "새 책 생성"은 하루 1회 제한에 걸려 **다음 날까지 아무것도 못 하는 상태**가 됐음 | 후보를 다시 조회하는 경로가 없었고(조회 API 부재), 재생성은 일일 제한에 막혔다. 게다가 `_produce_candidate_books`가 시작할 때 모든 `pending` 후보를 무조건 `pool`로 되돌려 다른 사용자의 선택 중인 후보까지 회수했다 | 1. `GET /api/books/candidates/pending` 신규 추가 — 아직 고르지 않은 후보를 돌려준다.<br>2. "새 책 생성"은 pending이 있으면 **새로 만들지 않고 그 후보를 그대로 반환**한다(일일 제한도 소모하지 않음). 프론트도 생성 전에 먼저 확인하고, 후보 화면에 직접 들어와도 비어 있으면 서버에서 복원한다.<br>3. pending 정리와 채택 후 회수를 **후보를 만든 사용자 기준**으로 좁혔다(기존 `created_by` 컬럼 활용 — 정의만 되어 있고 쓰이지 않던 컬럼이었다). |
| **UI (채팅방 공지가 눈에 띄지 않고, 답장 버튼 정렬이 어긋남)** | 독서방 상단 공지("독서방 개설일", "독서방이 열렸습니다")가 크림색 배경 위에서 탁한 회색으로 묻혀 보이고, 말풍선 아래 "답장 · 반응" 버튼이 말풍선 왼쪽 선과 어긋나 보였음 | ① 공지 pill 색상이 `rgba(0,0,0,0.18)`인데 그마저도 `makeDivider()`가 **인라인 스타일로 덮어써서** CSS 수정이 먹지 않았다. ② `.chat-action-toolbar` / `.chat-action-btn` / `.chat-action-divider`에는 **스타일 정의가 아예 없어** 브라우저 기본 버튼 모양이 그대로 노출됐다 | ① 인라인 스타일을 제거하고 공지를 종이 톤 앰버 라벨(배경 `--accent-bg`, 글자 `#8b5a2b`)로 교체 — 명암비 5.22로 WCAG AA 통과. 날짜 구분선은 색이 아니라 양옆 가는 선으로 역할을 구분했다.<br>② 답장/반응 툴바에 정렬·간격·호버 스타일을 정의하고 말풍선 왼쪽 선에 맞췄다. |
| **커밋 전 자체 코드리뷰에서 발견한 결함 일괄 수정** | 이번 세션 변경분(미커밋 69개 파일)을 5개 영역으로 나눠 병렬 리뷰하고 각 지적을 적대적으로 재검증한 결과, **확인 25건(높음 11)**. 이 중 이번 세션에 새로 넣은 코드의 결함을 커밋 전에 수정했다 | 대량 문자열 치환으로 코드를 적용하다 보니 문맥을 놓친 지점과, 역할 분리(is_bot 도입)의 파급 효과를 따라가지 못한 지점이 있었다 | **① 기동 시 계정 삭제 위험 제거**: `enforce_roles()`가 닉네임에 '사회자'가 들어간 계정을 중복 봇으로 보고 삭제하고 그 글을 공식 봇 소유로 이관했다. 닉네임으로 가입한 일반 회원의 계정·평점·서재가 재시작마다 영구 삭제되고 그 글이 사회자 발언으로 둔갑할 수 있었다. 판단 근거를 **봇 플래그로만** 좁히고, 삭제 대신 **봇 표시 해제**로 바꿨다.<br>**② 개발용 시크릿 고정 문자열 제거**: `APP_ENV` 미설정·오타 시 조용히 개발 모드로 기동해 소스에 박힌 JWT 키를 쓰던 문제 → 허용값 화이트리스트 검증(위반 시 기동 실패) + 개발용 대체 시크릿을 **매 기동 난수**로 생성.<br>**③ 관리자 이메일 하드코딩 제거**: 저장소에 기본값으로 박혀 있어, 해당 계정이 없는 동안 누구든 그 주소로 가입하면 다음 기동에 관리자 권한을 받아갈 수 있었다. 기본값을 없애고 미설정 시 권한 자동 부여/회수를 아예 건너뛰도록 했다(설정 누락으로 관리자가 사라지는 사고 방지).<br>**④ 참여자 수 오집계**: `is_admin == False`로만 걸러 역할 분리 후 AI 사회자가 참여 독자로 카운트됐다 → `is_bot == False` 조건 추가.<br>**⑤ 아카이브 월 접기가 실제로 접히지 않던 문제**: `.arc-month-grid { display: grid }`가 브라우저 기본 `[hidden]{display:none}`을 이겨서 상태만 바뀌고 카드가 그대로 보였다 → `[hidden]` 보정 규칙 추가.<br>**⑥ 비밀번호 변경 후 좀비 세션**: 서버는 기존 토큰을 무효화하는데 프론트는 로그인 상태를 유지해 이후 모든 요청이 401이 됐다 → 변경 성공 시 세션을 정리하고 로그인 화면으로 보낸다.<br>**⑦ 재설정 링크 제출이 발송 제한에 막히던 문제**: 메일 발송과 비밀번호 제출이 같은 레이트리밋을 공유해, 메일을 3번 요청하면 정작 링크를 눌러도 429가 났다 → 제출 전용 제한기를 분리하고 토큰 오류일 때만 시도를 기록한다.<br>**⑧ 옛 문구 잔존**: 비밀번호 찾기 모달만 "임시 비밀번호를 즉시 전송" 문구로 남아 실제 동작(재설정 링크 발송)과 달랐다 → 문구 정정 및 다국어 키 연결. |
| **데이터 정리 (중복 계정 · 테스트 흔적 · 닉네임 풀 확장)** | 전수 점검에서 ① 닉네임 `달빛독자`가 서로 다른 두 계정에 중복되어 있었고(현재 가입 로직은 중복을 막지만 그 이전에 생긴 레거시 데이터), ② 이번 검증 과정에서 만든 임시 계정과 테스트로 개설된 독서방이 남아 있었으며, ③ 닉네임 풀이 45개뿐이라 가입자가 늘면 소진될 여지가 있었음 | 닉네임 중복 검사는 회원가입 API에만 있고 기존 데이터를 정리하는 절차가 없었음. 닉네임 풀은 소진 시 Gemini가 30개씩 자동 충전하도록 되어 있으나, 그 경로는 API 호출과 파일 쓰기를 동반하므로 평소 여유분을 넉넉히 두는 편이 안전함 | 1. 중복 닉네임 계정을 **활동이 많은 쪽을 남기고** 병합 — 삭제되는 계정의 채팅은 남는 계정으로 이관해 대화 기록이 끊기지 않게 했다(평점·서재는 유니크 제약 충돌을 피해 삭제). 정리 후 닉네임 중복 0건.<br>2. 검증용 임시 계정 6개와 테스트로 개설된 독서방 1개를 삭제하고, 그 과정에서 상태가 바뀐 후보 도서를 원상 복구했다.<br>3. **닉네임 풀을 45개 → 100개로 확장**(서재·문장·시간대·상상 계열 55개 추가). 서버측 닉네임 규칙(한글/영문/숫자, 20자 이내)을 전부 통과하는지와 기존 가입자와 겹치지 않는지를 검증했다. 현재 가용 86개.<br>(배정 방식은 그대로: 풀에서 이미 가입된 닉네임을 제외한 뒤 무작위 선택. 다만 배정은 '예약'이 아니라서 동시에 가입 화면을 연 두 사람이 같은 닉네임을 받을 수 있고, 그 경우 가입 단계에서 중복으로 걸린다) |
| **도서 상세 페이지 재배치 (2단 구성 · 남은 기간 강조 · 통계 표시 정책 변경)** | (버그 아님, 개편) 데스크톱에서 본문 폭 제한이 없어 줄거리 한 줄이 **1,207px**까지 늘어나 눈이 줄을 따라가기 어려웠고, 첫 화면 19개 요소 중 값이 없는 통계(평균 평점 `—`, 채팅 반응 🔒)가 **248px**를 차지했다. 허구 설정인 쪽수·정가가 참여 버튼 바로 위에 놓여 실제 구매 정보처럼 읽혔고, 폰트 크기가 11종으로 갈려 장르·D-day·인원·태그가 비슷한 무게로 경쟁했다 | 화면 요소가 늘어나는 동안 "무엇을 먼저 보여줄지"에 대한 기준 없이 세로로 계속 덧붙여 온 결과. 데스크톱 폭이 넓어져도 본문이 그만큼 늘어나기만 했다 | **왼쪽은 판단·행동, 오른쪽은 읽을거리**로 나눈 2단 구성으로 재배치.<br>1. 왼쪽: 표지(240×336) · **남은 기간 카드** · 참여/담기 버튼 · 장르·인원 한 줄. 오른쪽: 제목 · 작가 · 줄거리(660px 제한) · 추천사 · 출판사 서평 · 가상 판권면.<br>2. **남은 기간 카드** 신설 — 큰 숫자만으로는 급한 정도가 가늠되지 않으므로 진행 바(10일 중 N일 지남)와 종료 예정일을 함께 표시하고, 남은 기간에 따라 문구를 바꾼다(여유: "처음부터 함께" / 임박: "오늘 남긴 감상이 아카이브에 실립니다"). 색은 홈 카드와 같은 신호등 규칙.<br>3. **통계 표시 정책 변경** — 반응 집계는 진행 중에도 한 줄로 공개(채팅에서 이미 메시지별로 보이던 값이라 가릴 이유가 없고, 없으면 "아직 반응이 없어요 · 첫 감상을 남겨보세요"로 유입 문구를 노출). 평균 평점만 접어서 봉인한다(진행 중 평균이 보이면 서로의 점수에 끌려가고, 방의 성격이 감상 나누기에서 평가하기로 바뀐다). 248px → 90px.<br>4. 쪽수·정가·주제어를 **「가상 판권면」**으로 분리해 실제 CTA와 섞이지 않게 했다.<br>5. 좌우 열 높이는 고정값이 아니라 남는 공간을 흡수하는 방식으로 맞췄다 — 줄거리 길이가 책마다 다르므로(163~240자 실측) 고정 높이로는 어긋난다. 5권으로 검증해 **바닥 정렬 차이 0px** 확인. |
| **도서 상세 레이아웃 기준 정립 (간격·정렬선 통일)** | 위 재배치 과정에서 구획마다 좌우 패딩이 제각각(헤더 28px / 통계 22px / 목차 22px / 명예의 전당은 인라인 `margin: 24px 22px`)이라 **정렬선이 네 갈래**로 갈라졌고, 한 곳을 고치면 다른 곳이 어긋나는 일이 반복됐다 | 공통 레이아웃 규칙 없이 요소별로 패딩·마진을 개별 지정해 온 구조. 여기에 `#pg-detail`이 flex 컨테이너라 자식에 `margin:auto`를 주면 stretch가 풀려 폭이 내용 크기로 줄어드는 함정이 겹쳤다(명예의 전당이 639px로 줄어든 원인) | 페이지의 모든 간격을 **변수 두 개**에서만 파생하도록 정리했다.<br>`--detail-content-width: 1000px`(= `.detail-top` 그리드 폭과 반드시 동일) / `--detail-gap: 32px`.<br>구획은 배경을 화면 끝까지 깔고 **안쪽 내용만** 폭을 제한한다(자식에서 처리해 flex stretch가 풀리지 않게). 인라인 스타일로 박혀 있던 명예의 전당 마진은 CSS로 옮겼다.<br>결과: 뒤로가기·헤더 2단·반응 줄·평점 줄·통계·목차·명예의 전당 **7개 요소가 좌우 213px에서 일치**, 구획 상하 여백 32px로 통일. |
| **UI 결함 4건 (작업 중 자체 발견 및 사용자 지적)** | ① 「가상 판권면」을 접은 채로 만들어 쪽수·정가·주제어가 사라진 것처럼 보임 ② 추천사 글자를 흰색으로 바꿨는데 배경이 밝은 크림색이라 **명암비 1.12로 사실상 안 보임** ③ 같은 실수로 "아직 반응이 없어요" 문구도 흰 배경에 흰 글씨 ④ 판권면을 헤더 밖에 베이지색 블록으로 두어 **갈색→베이지→흰색 3중 경계**가 생김 | 어두운 헤더용으로 만들어진 스타일(`.detail-price`, `.detail-tag`, `.endorse-quote`)을 밝은 배경으로 옮기면서 색을 확인하지 않았다. 배경 위에 얹는 요소의 대비를 검증하지 않는 같은 패턴이 반복됐다 | 판권면을 기본 펼침으로 바꾸고 헤더 안(오른쪽 열 끝)으로 이동해 배경 경계를 다시 1개로 되돌렸다. 밝은 배경으로 옮긴 요소들의 글자색을 재지정(추천사 1.12 → **12.35**, 판권면 정가·쪽수 1.0 → **9.0**, 빈 반응 문구 1.0 → **4.14**). D-day 카드 배경도 흰 글씨와의 대비가 3.19로 기준 미달이라 한 단계 진한 색으로 조정(**5.02**).<br>이후 레이아웃 변경 시에는 수치 측정과 함께 배경·대비·정렬선·좌우 균형을 한 묶음으로 확인하기로 했다. |
| **안 쓰는 코드 정리** | (버그 아님, 정리) 개편을 거치며 호출부가 사라진 코드가 누적되어 있었다 | 기능을 교체할 때 새 코드만 넣고 이전 코드를 남겨 두는 일이 반복됐다 | 참조가 **0건인 것만** 골라 제거했다. JS 함수 4개(`getAllGenres`·`renderArcHomeSection`·`toggleChatSearch`·`tsNow`), CSS 규칙 25개(`.dday-badge`·`.dday-row`·`.endorse-block`·`.cover-shimmer` 등), 파이썬 함수 2개(`_generate_abstract_cover_pil`·`build_cover_prompt`)와 그 제거로 연쇄적으로 미사용이 된 헬퍼 6개(`_hex_to_rgb`·`_draw_gradient_bg`·`_analyze_book`·`_pick_palette`·`_pick_composition`·`_lerp_color`), 다른 키로 대체된 중복 i18n 키 20개.<br>동시에 사전에만 있고 연결되지 않았던 **아카이브 라벨 13곳을 t()로 연결**해, 일본어 전환 시 아카이브 통계까지 번역되도록 했다(参加読者 / チャットメッセージ / コメント / 反応の総数 / 平均評価 확인).<br>결과: main.py 3,403 → **2,988줄**, styles.css 5,799 → 5,644줄, books.js 4,488 → 4,437줄. |
| **UI 검토용 목업 5종 작성** | (문서) 화면 개편 방향을 코드로 옮기기 전에 눈으로 합의할 수단이 없었다 | — | `static/mockups/`에 실제 디자인 토큰과 PC 화면(1400×760) 기준으로 작성. 도서 상세 재배치안, 아카이브 재배치안, 아카이브 방향 3안 비교, 웹 매거진 최소 변경안, 그리고 **실제 DB 데이터로 채운 예시**와 **참여자 30명 가상 데이터 예시**.<br>실제 데이터로 그려본 결과 이 서비스의 아카이브가 **반응 0건 · 평점 0건**이라는 점이 드러나, 목업에서 그럴듯해 보였던 「최다 공감 리본」이 실제로는 붙일 근거가 없다는 것을 확인했다. 화면 설계를 가상 수치로 검토할 때의 함정을 기록해 둔다. |

### 🧱 이번 작업으로 신설된 모듈

| 파일 | 역할 |
|---|---|
| `config.py` | 모든 환경변수의 유일한 진입점. 운영에서 필수 시크릿 누락 시 기동 중단 |
| `security.py` | `RateLimiter`(슬라이딩 윈도우), `SecurityHeadersMiddleware`(CSP 등), `client_ip()` |
| `auth.py` (재구성) | 인증·인가 전담. 엔드포인트는 `get_current_user` / `get_current_user_id` / `require_admin` 세 의존성만 사용 |
| `templates/reset_password.html` | 메일 링크로 진입하는 비밀번호 재설정 페이지 |
| `.env.example` | 커밋 가능한 설정 템플릿 (실제 `.env`는 계속 `.gitignore` 대상) |
| `static/css/mobile.css` | 모바일 대응 레이어. styles.css 뒤에 로드되어 덮어쓰기만 한다 (태블릿 900 / 모바일 640 / 소형 340px) |

### ✅ 8월 18일 "보고만 완료" 항목 처리 결과
- **이메일 열거 가능성** → 이번 작업의 비밀번호 재설정 항목에서 해결 (가입 여부와 무관하게 동일 응답)
- **인증 없는 관리용 API(auto-archive)** → 이번 작업의 인가 검사 일원화 항목에서 해결 (관리자 전용)
- **`_login_fail_attempts` 인메모리 무제한 증가** → 이번 작업의 무차별 대입 방어 항목에서 해결 (`RateLimiter`가 윈도우 밖 기록을 자동 정리)
- **`toggleChatSearch()` 데드 코드** → 미조치 (기능 영향 없음, 별도 정리 대상으로 유지)

### 🔍 이번에도 조치하지 않고 보고만 한 항목
- **토큰의 localStorage 저장**: XSS 삽입 지점을 전수 차단해 실질 위험은 크게 낮아졌으나, 근본적으로는 httpOnly 쿠키 + CSRF 토큰 방식이 안전함. 인증 흐름 전반을 바꿔야 하므로 별도 작업으로 분리.
- **CSP의 script-src unsafe-inline**: 템플릿과 `books.js`가 인라인 onclick 핸들러를 광범위하게 사용하고 있어 현재는 제거할 수 없음. 이벤트 위임 방식으로 전환하면 삭제 가능.
- **`cleanup_users.py`**: 특정 계정만 남기고 전체 사용자를 연쇄 삭제하는 스크립트가 확인 절차 없이 리포지토리에 존재.
- **`requirements.txt` 버전 미고정**: 재현 가능한 빌드 및 취약 버전 추적이 어려움.

커밋 전 자체 코드리뷰(확인 25건)에서 실재가 확인됐으나 이번 커밋 범위 밖으로 남긴 항목:
- **`X-Forwarded-For` 무조건 신뢰**: `security.client_ip()`가 프록시 신뢰 여부를 검증하지 않아, 헤더를 매 요청 바꾸면 가입·로그인 IP 제한이 무력화된다. 계정 기준 제한은 그대로 동작하므로 실제로 열리는 것은 '대량 가입'과 '여러 계정에 걸친 비밀번호 추측'이다. 프록시 뒤에 배포할 때 `TRUST_PROXY` 설정과 함께 처리해야 한다.
- **표지 폴백 실패를 무시**: `generate_book_cover_art()`가 `_generate_editorial_cover_pil()`의 실패(`False`) 반환을 받지 않고 URL을 반환한다. 폴백까지 실패하면 존재하지 않는 파일 경로가 DB에 저장될 수 있다(이번 세션 변경으로 생긴 회귀).
- **표지 5KB 휴리스틱 오탐**: "5KB 미만이면 저품질"로 보는 기존 판정이, 폴백 렌더러 산출물(실측 2.3~4.9KB)을 정상인데도 실패로 오인한다. 예전 표지는 51~104KB였기에 드러나지 않던 문제.
- **아카이브 매거진의 도서 메타데이터 미이스케이프**: `openArchive()`에서 사용자 발화는 이스케이프했지만 제목·작가·태그·인용구 블록이 남아 있다(books.js 2893~3066 부근).
- **i18n 사전 키 54개가 미연결**: 사전에는 한/일 값이 있으나 호출부가 없어 여전히 하드코딩된 구간이 있다(아카이브 통계 라벨 등).
- **`.chat-search-arrow` 중복 정의**: 같은 특이성의 구버전 블록이 뒤에 있어 신규 스타일(크기·색·전환)을 덮어쓴다.
- **안내/약관 페이지 마크업 이중 보유**: 같은 HTML이 템플릿과 `I18N_DICT` 양쪽에 존재해, 한쪽만 고치면 언어 전환 시 내용이 어긋난다.
- **`_CREDENTIALS_EXCEPTION` 재사용**: 모듈 전역 예외 객체를 401 경로 5곳에서 다시 raise해 트레이스백 프레임이 누적된다(401 1회당 약 10프레임).

---

## 📅 2026년 8월 25일 (채팅 API 전면 장애 복구, 반응 집계 서버 이관, 채팅 이모지 정책 적용 & 독서방·아카이브 콘텐츠 시드)

> 전날 정리 커밋이 상수 정의를 함께 지워 **채팅 API가 모든 방에서 500을 내고 있었다**. 이를 복구하는 과정에서 「지금 이 방의 반응」이 애초에 동작한 적 없다는 사실이 드러나 집계 책임을 서버로 옮겼다. 이어 사용자 지적에 따라 채팅 본문의 이모지를 걷어내고, 비어 있던 독서방과 아카이브에 대화·반응·평점을 시드해 화면이 실제 데이터로 검증 가능한 상태를 만들었다.

| 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|
| **크리티컬 — 채팅 API 전면 500 (`MODERATOR_EMAIL` 미정의)** | 모든 독서방에서 `GET /api/books/{id}/chats`가 **500 Internal Server Error**를 반환했다. 채팅이 한 건도 로드되지 않아 독서방을 열 수 없었고, 채팅 캐시에 의존하던 도서 상세의 반응 집계도 함께 죽어 있었다 | 8월 24일 「안 쓰는 코드 정리」 커밋(`f7ff8ec`)에서 `MODERATOR_EMAIL` / `MODERATOR_NICKNAME` **상수 정의 블록을 참조가 없는 코드로 오판해 삭제**했다. 두 상수는 `get_or_create_moderator()` 등 7곳에서 쓰이고 있었으나, 삭제 판단을 정의부 주변만 보고 내렸다. 파이썬은 미정의 전역을 **호출 시점에야** `NameError`로 알리므로 기동은 정상적으로 되어 발견이 늦었다 | 두 상수를 모듈 상단(모든 사용처보다 앞)에 복원하고 용도 주석을 달았다.<br>재발 방지로 **AST 기반 점검**을 수행했다 — `HEAD~2`와 현재의 모듈 최상위 이름을 비교해 "삭제됐지만 아직 참조되는 이름"을 추출했고, 나머지 8개는 실제로 미참조임을 확인했다. 이번 사고의 교훈은 *"정리 커밋 뒤에는 실행 경로를 한 번 밟아본다"* 로, 삭제 후 채팅·상세·아카이브 주요 API를 직접 호출해 200을 확인하는 절차를 거쳤다. |
| **도서 상세의 반응 집계가 항상 0 (집계 출처가 클라이언트 캐시)** | 「지금 이 방의 반응」 줄이 실제 반응이 131개 쌓인 방에서도 **"아직 반응이 없어요"** 로 표시됐다 | `getRxTotals()`가 `chatMsgs[book.id]` — **채팅방을 실제로 열어야 채워지는 프론트 캐시** — 를 훑어 합산했다. 도서 상세는 채팅을 로드하지 않으므로 캐시가 비어 있어 항상 0이었다. 즉 데이터가 아니라 **집계 위치**가 잘못된 구조적 결함이었고, 8월 24일 신설한 반응 줄은 사실상 동작한 적이 없었다 | 집계 책임을 **서버로 옮겼다**.<br>1. `aggregate_reactions(db, book_ids)` 신설 — 반응은 `ChatMessage.reactions`에 JSON 문자열로 저장돼 SQL 집계가 불가능하므로 파이썬에서 합산하되, `reactions`가 비어 있지 않은 행만 조회한다.<br>2. 목록 응답(`/api/books`, `/api/books/archived`)은 도서 id를 모아 **한 번만** 질의해 N+1을 피하고, 상세 응답은 해당 도서만 집계한다. 결과를 `rx_counts`로 실어 보낸다.<br>3. 프론트는 `dbBook.rx_counts`를 그대로 쓰고, 채팅방을 열어 둔 동안 생긴 즉시 반응만 캐시 값이 더 클 때 반영해 이중 계산을 막는다.<br>4. 아카이브 분기에 있던 캐시 재집계 코드는 서버 값을 덮어쓰므로 제거했다. |
| **채팅 본문에 이모지 사용 (서비스 톤 위반)** | 사용자 지적: *"채팅방에서 이모지는 쓰지않아 이모티콘도"*. 채팅 메시지 본문에 🎙️ ✨ 😭 ⚓️ 등이 섞여 있었고, 사회자 웰컴 카드는 질문 번호를 `1️⃣ 2️⃣ 3️⃣` 키캡 이모지로 매기고 있었다 | 감정 표현은 메시지에 다는 **반응 기능(❤️ 🤔 😄 ✨)** 이 담당하는데, 본문에까지 이모지를 쓰면 역할이 겹치고 톤이 흐트러진다. 시드 대화·웰컴 템플릿·AI 사회자 프롬프트 어디에도 금지 규칙이 없어 세 경로 모두에서 이모지가 유입됐다 | 유입 경로를 전부 막았다.<br>1. **AI 사회자 프롬프트 2종**(멘션 응답 · 자발적 질문)에 "이모지와 이모티콘을 절대 사용하지 마세요" 규칙 추가.<br>2. **서버측 안전망** `strip_chat_emoji()` 신설 — 모델이 규칙을 어겨도 DB 저장 직전에 제거한다. 키캡 결합문자(U+20E3)·변이 선택자(U+FE0F)·ZWJ까지 포함하고, 이모지가 빠진 자리의 겹공백·문장부호 앞 공백을 정리한다.<br>3. **웰컴 카드 템플릿**: `1️⃣` → `1.`, 머리말의 🎙️ 제거.<br>4. `main.py`의 하드코딩 시드 채팅 26줄, `books.js`의 샘플 채팅 26줄, **DB 채팅 42건**을 정리했다.<br>검증: 전체 독서방 **107개 메시지에 이모지 0건**. 반응 데이터(`reactions` JSON)는 손상 0건으로 확인했다.<br>적용 범위는 **채팅 본문에 한정**했다 — 토스트 메시지, 프롬프트 내부 지시 기호(⚠️ 💡), 안내 페이지, 사회자 아바타 아이콘은 채팅 본문이 아니므로 유지했다. |
| **빈 독서방·빈 아카이브 (실사용 데이터 부재)** | 활성 독서방 4곳 중 3곳이 **대화 0~2건**, 아카이브 10권은 **전권 반응 0 · 평점 0**이었다. 8월 24일 재배치한 상세 화면의 반응 줄·평점 줄·참여자 수가 모두 빈 상태로만 보여, UI 개편의 효과를 화면에서 확인할 수 없었다 | 이 서비스는 아직 실사용자가 없어(계정 85개 중 대부분이 시드·테스트) 콘텐츠가 자연 발생하지 않는다. 화면을 아무리 다듬어도 **표시할 데이터가 없으면 검증이 불가능**하다는 점이 8월 24일 목업 검토에서 이미 드러났다("최다 공감 리본을 붙일 근거가 없다") | 활성 3방 + 아카이브 10권에 3일치 대화·반응·평점을 시드했다.<br>**작성 원칙** — ① 감상은 각 도서의 줄거리·인물·명대사·핵심 질문을 근거로 쓰고, ② 한 방에 **찬반이 갈리는 의견을 섞어** 사회자가 개입할 여지를 만들며, ③ 답글(`reply_to_id`)로 실제 토론 흐름을 만들고, ④ 타임스탬프를 3일에 걸쳐 분산시키고, ⑤ 닉네임은 **`nicknames_pool.json` 안에서만** 골라 실제 배정 규칙과 어긋나지 않게 했다(풀 밖 이름으로 만들었던 계정 2개는 풀 안 이름으로 교정).<br>평점은 그 방에서 **실제로 대화한 독자에게만** 배정하고 봇·관리자는 제외했으며, 책마다 분포를 달리해 호불호가 갈린 작품(평균 3.6)도 만들었다.<br>결과: 활성 4방 **참여 6~7명 · 반응 112~179 · 평점 6~8건**, 아카이브 10권 **반응 합계 1,331 · 평점 합계 59건**(평균 3.6~4.8). 기존 반응·평점은 덮어쓰지 않도록 조건을 걸었다. |

## 💡 주요 시스템 건축 및 기술적 성과 요약

> [!NOTE]
> **모듈화 템플릿 구조 (Jinja2 Templates)**
> 메인 뼈대 `index.html`을 중심으로 `components/`(네비게이션, 글로벌 푸터)와 `pages/`(홈, 후보 생성, 도서 상세, 채팅방, 내 서재, 이용 안내 등) 모듈로 체계적으로 분리하여 가독성과 유지보수 편의성을 극대화했습니다.

> [!TIP]
> **온디맨드 REST API 아키텍처**
> 첫 진입 시 무거운 데이터 전체를 로딩하지 않고, 사용자가 클릭하거나 이동하는 시점(온디맨드)에 단일 도서 상세 API(`/api/books/{id}`) 및 아카이브 API를 호출하여 네트워크 트래픽을 아끼고 속도를 극대화했습니다.

> [!IMPORTANT]
> **후보 도서 보관함(Pool) 무작위 랜덤(Random Mix) 순환 관리**
> 3권 중 선택받지 못한 후보 도서를 DB 보관함(`pool`)으로 자동 수집하고, "다음 책 생성" 시 보관함 속 도서들 중에서 매번 무작위 랜덤(`func.random()`)으로 꺼내어 매번 다채롭고 신선한 3권 후보 조합을 보여주도록 구현했습니다.
