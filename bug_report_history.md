# 📚 가공독서회 (Gakong Reading Club) 버그 수정 및 개선 이력 보고서 (날짜별 정원)

본 문서는 **가공독서회 (Gakong Reading Club)** 프로젝트의 개발 및 유지보수 과정에서 처리된 모든 디버깅, 버그 수정, UX 개선 내역을 **날짜별(과거 ➔ 최신)**로 체계적으로 정리한 종합 보고서입니다.

---

## 📅 2026년 7월 15일 (초기 시스템 연동 및 기본 UX 안정화)

| 번호 | 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|---|
| **1.1** | **데이터 동기화** | 로컬 `BOOKS` 배열 데이터 불일치 | 프론트엔드(`books.js`) 상단에 하드코딩된 더미 `BOOKS` 배열이 노출되어 DB의 실시간 도서 정보와 연동되지 않음 | 애플리케이션 로딩 시 `/api/books` API를 조회하여 로컬 `BOOKS` 배열을 동적으로 로드 및 매핑하도록 수정 |
| **1.2** | **UX 피드백** | 댓글 실시간 글자수 제한 피드백 누락 | 140자 입력 제한 카운터가 없어 사용자가 길게 입력 시 사전 안내 없이 API 요청 실패 | HTML에 카운터 요소를 배치하고 JS `oninput` 이벤트를 통해 글자수를 실시간 표시. 130자 이상 입력 시 경고색(붉은색) 적용 |
| **1.3** | **권한 관리** | 새로고침 시 댓글 수정/삭제 버튼 유실 | 댓글 상세 목록을 재로드하는 `openChat()` 실행 시 `mine: true` 속성 매핑이 누락되어 작성자 제어권 유실 | `openChat()` 내부에서 현재 로그인한 유저 ID(`CURRENT_USER_ID`)와 작성자 ID(`c.userId`)를 비교해 `mine: true` 프로퍼티 동적 보존 |
| **1.4** | **API 연동** | 평점 취소(0점) DB 미반영 | 사용자가 별점을 0점으로 변경했을 때 `book.myRating > 0` 조건식으로 인해 서버 API 호출 건너뜀 | 별점이 0점(취소)인 경우에도 평점 API가 정상 호출되도록 프론트엔드 조건식 완화하여 DB 동기화 완료 |
| **1.5** | **타입 바인딩** | 아카이브 상세 페이지 진입 실패 | 아카이브 탭 클릭 시 DB 정수형 ID와 프론트엔드 문자열 ID 타입 불일치(`===`)로 빈 화면 출력 | `parseInt()` 형변환을 적용하여 다양한 타입 입력에서도 안전하게 상세 뷰 모달이 활성화되도록 타입 안정성 확보 |

---

## 📅 2026년 7월 16일 (인증 세션 및 백엔드 예외 처리 강화)

| 번호 | 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|---|
| **1.6** | **세션 소거** | 로그아웃 후 세션 정보 잔존 | `logout()`과 `handleLogout()` 혼용으로 일부 로그아웃 로직에서 개인정보 삭제 누락 | 로그아웃 프로세스를 `handleLogout()`으로 통합하여 세션 정보(토큰, 이메일, 닉네임, ID)를 완전 소거하도록 일원화 |
| **1.7** | **경고 해결** | FastAPI Startup event 및 UTC 시간 경고 | FastAPI `@app.on_event("startup")` 데코레이터와 파이썬 `datetime.utcnow()`의 Deprecated 콘솔 경고 발생 | `on_event`를 FastAPI `lifespan` 컨텍스트 매니저로 교체하고 파이썬 3.12 스펙에 맞춰 `datetime.now(timezone.utc)`로 업데이트 |
| **1.8** | **UX 개선** | 책 채택 완료 후 탐색 단절 | 후보 도서 채택(`adoptCandidate`) 후 메인 화면으로 이동하여 개설된 독서방 확인을 위한 추가 탐색 필요 | 채택 성공 시 `renderHome()` 목록 동기화 후 `openDetail(newAdapted.id)`을 호출해 새로 개설된 도서 상세 페이지로 자동 이동 |
| **1.9** | **예외 복구** | 새 책 후보 생성 도중 백엔드 `NameError` | AI 도서 생성 과정 예외 발생 시 fallback 데이터를 불러오려 하지만 `GENRE_FALLBACKS` 미정의로 크래시 발생 | `main.py` 상단에 각 장르별 템플릿과 추천사 세트를 결합한 `GENRE_FALLBACKS` 상수를 선언하여 API 비가동 시에도 안전한 fallback 보장 |

---

## 📅 2026년 7월 17일 (도서 몰입 보드 도입 및 스키마 검증)

| 번호 | 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|---|
| **1.10** | **구조 개선** | 거대 단일 HTML 파일 유지보수 한계 | 단일 2,600줄 스탠드얼론 HTML 파일로 인해 화면별 유지보수 및 협업 난항 | FastAPI `Jinja2Templates` 연동 및 `templates/` 하위 모듈화 분리 (`index.html`, `pages/`, `components/`) |
| **1.11** | **데이터 매핑** | AI 도서 상세 몰입 데이터(JSON) 연동 누락 | 백엔드 Gemini API 및 Fallback을 통해 생성된 `immersion_data` 컬럼이 프론트엔드 어댑터 매핑 누락으로 미노출 | `books.js` 어댑터 함수에 `immersionData` 객체 파싱(JSON.parse) 및 상세 화면(`openDetail`) 4개 탭(목차, 관계도, 서평, 비하인드) 동적 바인딩 |
| **1.12** | **스키마 검증** | Pydantic validation 422 및 Rating deletion 404 | 백엔드 `RatingCreate`(`score`)와 `ChatCreate`(`text`) 스키마 필드 매핑 및 테스트 데이터 타입 불일치 | 프론트엔드-백엔드 데이터 계약 검증(`test_contract.py`) 완료, DELETE endpoint(`/api/books/{id}/rate`) 및 AI Moderator 싱크 완료 |

---

## 📅 2026년 7월 20일 (DOM ID 보완 및 실시간 AI 싱크)

| 번호 | 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|---|
| **1.13** | **DOM & 동기화** | UI DOM ID 누락 및 채팅 하드코딩 봇 충돌 | `detail.html`, `library.html`에서 일부 ID 누락 및 프론트엔드 하드코딩 봇 응답이 실시간 백엔드 AI Moderator와 충돌 | HTML 템플릿 ID 보완, `auth.js` `updateAuthUI()` 로그인 유저 동적 렌더링, `books.js` 하드코딩 봇 제거 후 백엔드 AI Moderator와 완전 동기화 |

---

## 📅 2026년 7월 21일 (글로벌 푸터, 권한 게이트, 온디맨드 API 및 전수 점검)

| 번호 | 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|---|
| **1.14** | **템플릿 누락** | '새 책 생성' 등 클릭 이벤트 미작동 | Jinja2 모듈화 템플릿 `templates/index.html` 하단에 JS 모듈(`auth.js`, `books.js`) `<script>` 태그 누락 | `templates/index.html` 하단에 `<script>` 태그 추가 및 E2E 브라우저 자동화 검증 완료 (새 책 생성, AI 후보 생성, 채택 독서방 개설 정상 작동) |
| **1.15** | **UI/UX 컴포넌트** | 글로벌 푸터 컴포넌트 추가 및 라우팅 | 하단 전역 푸터 누락 | `templates/components/footer.html` 신규 생성 (1:1 문의, 이용 안내, 개인정보처리방침, 저작권 표시 4개 항목), `goPage()` 페이지 전환 브라우저 테스트 완료 |
| **1.16** | **권한 검증** | '새 책 생성' 비로그인 사용자 허용 문제 | 비로그인 상태에서도 새 책 생성(후보 3종 소환)이 가능했던 권한 미검증 | `books.js` 내 `generateBook()` 시작 부분에 인증 토큰(`token`) 검증 로직 추가. 미인증 시 알림(`새 책을 소환하려면 먼저 로그인해 주세요! 📖`) 및 `auth-gate` 화면으로 자동 이동 |
| **1.17** | **성능 최적화** | 페이지별/상황별 API 온디맨드(On-Demand) 분리 | 초기 로딩 시 전체 도서 및 아카이브 도서 데이터까지 일괄 수신하여 로딩 속도 저하 | `GET /api/books`(홈 활성 도서), `GET /api/books/{id}`(도서 상세 온디맨드), `GET /api/books/archived`(아카이브 탭 클릭 시), `GET /api/users/me/library`(서재 탭 진입 시)로 분리하여 로딩 속도 극대화 |
| **1.18** | **전수 정밀 점검** | 전체 코드베이스 구문/템플릿/데이터 전수 점검 | 코드 꼬임 또는 병합 충돌 가능성 정밀 점검 요청 | Python 백엔드 모듈 5종 전수 컴파일(`[OK]`), Node.js 기반 JS 구문 검사(`[OK]`), HTML Jinja2 템플릿 태그 무결성 점검(`[OK]`), JSON 정구성 검사(`[OK]`) 완료. 코드 꼬임 없음 검증 |
| **1.19** | **신규 UX 기능** | 👑 명예의 전당 (베스트 상상 독자 리뷰) 탑재 | 종결된 아카이브 독서방의 탐색 재미 및 유저 참여 인정 동기 부여 요소 부재 | `templates/pages/detail.html` 및 `books.js`에 **`👑 명예의 전당 · 베스트 상상 독자 리뷰`** 골드 위젯 구현. 순위 뱃지(🥇 1위, 🥈 2위), 공감수, 독자 상상 문장, AI 작가 한줄평 동적 바인딩 및 브라우저 E2E 검증 완료. |
| **1.20** | **콘텐츠 고도화** | 📖 도서 상세 목차 문학성 및 표준국어대사전 어휘 고도화 | 천편일률적이었던 기본 목차(시작되는 여정, 짙어지는 안개 등)의 밋밋함 개선 | `books.js` 내 `generateRichTableOfContents()` 탑재. 국립국어원 표준국어대사전 문학 어휘(유영, 궤적, 섭리, 잔상, 찰나, 파문 등)를 활용하여 각 도서의 장르와 줄거리에 맞춘 3~5장 분량의 고품질 문학 목차 및 줄거리 동적 바인딩 완성. |
| **1.21** | **감정 묘사 고도화** | 🎭 <인간의 130가지 감정 표현법> 기반 묘사 기법 도입 | 단순 서사 요약에 머물던 베스트 상상 독자 리뷰 및 도서 서사 표현의 입체감 보강 | 참고 자료(티스토리 소설 감정 표현 모음)의 작가용 감정 Thesaurus 적용. **신체적 반응**(손끝의 경련, 턱 막히는 목구멍, 뜨거워지는 눈시울), **내적 동요**(서늘한 죄책감, 소용돌이치는 그리움, 덜컥 내려앉는 가슴), **파워 동사**(응시하다, 짓눌리다, 옥죄다) 표현을 `fallbackBestReviews` 및 서사 묘사 전반에 세밀하게 녹여내어 독자 몰입도 극대화. |
| **1.22** | **서사 기법 고도화** | 📚 현대소설 핵심 창작 이론(서술/묘사, 액자식 구성, 복선과 암시) 적용 | 단조로운 사건 열거에 머무르던 도서 챕터/서평의 문학적 깊이 강화 요청 | 현대소설 필수 개념(서술vs묘사, 액자식 구성, 말하기vs보여주기, 복선과 암시, 의식의 흐름)을 도서 상세 몰입 목차 및 AI 작가 한줄평 구조에 체계적으로 접목. 외부 이야기와 내부 이야기를 가로지르는 은유와 찰나의 장면 묘사를 완성하여 전문적인 문학적 완성도 확보. |
| **1.23** | **UX 가독성 개선** | 💡 누구나 쉽게 즐길 수 있는 직관적이고 친근한 서사 설명 개편 | 한자어(기하학, 비대칭, 멸실, 각인 등) 및 과도하게 서사적인 문체로 인한 진입 장벽 해소 요청 | 어려운 학술/한자 용어를 완전히 배제하고, 독자가 첫눈에 직관적으로 이해하고 흥미를 느낄 수 있는 따뜻하고 쉽고 몰입감 넘치는 문체로 도서 상세 목차 및 요약 문장 전면 순화 완성. |

---

## 📅 2026년 7월 30일 (후보 도서 보관함(Pool) FIFO 선입선출 순차 소진 알고리즘 적용)

| 번호 | 분류 | 버그 내용 및 증상 | 원인 | 수정 조치 사항 |
|---|---|---|---|---|
| **1.24** | **보관 DB 최적화** | 후보 도서 재사용 시 특정 책이 소진되지 않고 DB에 영구 잔여 | `random.sample` 무작위 추출로 인한 특정 도서 표류 현상(Starvation) 및 미채택 `pending` 도서의 보관함(`pool`) 전환 누락 | 1. `generate_candidates` 진입 시 미채택 `pending` 도서를 먼저 `pool`로 자동 집계.<br>2. 추출 알고리즘을 선입선출(FIFO, `created_at.asc()`, `id.asc()`)로 전환하여 오래 보관된 책부터 순차적으로 남김없이 100% 소진되도록 개선.<br>3. `pool`에 3권 이상 축적 시 Gemini API 생성을 생략하고 보관 도서를 최우선 반환하여 소진 속도 및 API 성능 극대화. |
| **1.25** | **타입 바인딩 & 전수 점검** | `openDetail(id)` 문자열 ID 전달 시 엄격 비교(===) 실패 가능성 예방 및 전체 시스템 안정성 점검 | 단일 도서 상세 온디맨드 로딩 시 문자열 ID 전달 상황 및 전체 백엔드/프론트엔드 구문 검증 필요 | 1. `openDetail` 함수 상단에 `parseInt(id, 10)` 타입 안전 가드를 선제 배치하여 다양한 파라미터 전달 환경에서 모달 활성화 보장.<br>2. Python 백엔드 모듈 5종 전수 컴파일 및 JS 2종 전수 파싱 검사 수행하여 구문/버그 없음 검증 완료. |
| **1.26** | **보관 DB 무작위 랜덤 믹스** | "새 책 생성" 시 남겨진 보관 도서(`status='pool'`)들 중에서 매번 무작위 랜덤(`func.random()`)으로 추출하도록 쿼리 개편 | 기존 고정 순서(FIFO) 추출 방식에서 탈피하여 매번 신선하고 다채로운 조합으로 3권 후보 도서가 매칭되도록 요구 | `main.py`의 `generate_candidates` 쿼리를 `order_by(func.random()).limit(3)`으로 업데이트하여 보관 DB 도서 중 무작위 랜덤으로 조합되어 매번 신선하게 추출되도록 조치. |
| **1.27** | **이용 안내 AI 사회자 섹션 반영** | 상단 네비게이션 `이용 안내` 페이지에 AI 사회자의 정체성과 3가지 주요 역할 안내 섹션 추가 | AI 사회자의 역할(추천 질문 제시, 담백한 퍼실리테이터, @사회자 멘션 1:1 대화)을 독자들이 한눈에 파악할 수 있도록 가이드 반영 요구 | `guide_and_info.html`에 `🎙️ AI 사회자 안내` 카드 섹션을 통일된 UI 디자인으로 반영하여 독자 접근성 강화. |
| **1.28** | **글로벌 푸터 폰트 크기 확대** | 하단 글로벌 푸터의 링크 4개(`1:1 문의`, `이용 안내`, `이용약관`, `개인정보처리방침`) 폰트 크기 확대 | 기존 12px 소형 글꼴에서 14.5px의 가독성 높은 폰트 크기로 가독성 개선 요구 | `styles.css`의 `.home-footer-link` 폰트 크기를 `14.5px !important`, `font-weight: 600`으로 키우고 안쪽 여백과 구분자 폰트까지 시원시원하게 조정 완료. |
| **1.29** | **Gemini AI 책 표지 생성 탑재** | 가공 책 생성 시 Gemini / Imagen 3 AI 표지 일러스트(`cover_image_url`) 자동 생성 및 UI 연동 | 기존 단색 컬러 표지 대신 나노 바나나 감성의 고화질 문학 파스텔 일러스트 표지가 자동 렌더링되도록 기능 탑재 요구 | 1. DB `books` 및 `candidate_books` 스키마에 `cover_image_url` 컬럼 마이그레이션.<br>2. `main.py`에 `generate_book_cover_art` 비동기 생성 엔진 구축하여 Imagen 3 / 파스텔 일러스트 렌더링 체인 탑재.<br>3. `books.js`의 메인 카드, 모달 상세 커버, 후보 카드에 AI 표지 아트 노출 연동 완료. |
| **1.30** | **한국어 / 일본어 다국어 스위처** | 상단 네비게이션에 `한국어 / 일본어` 언어 토글 스위처 및 i18n 문학 번역 엔진 탑재 | 클릭 시 사이트 주요 인터페이스(네비, 히어로 타이틀, 버튼, 푸터 등)가 정갈한 일본어(日本語)로 즉시 번역 전환되도록 요구 | 1. `nav.html` 및 `styles.css`에 `한국어 / 일본어` 스위처 버튼 UI 구현.<br>2. `books.js`에 `I18N_DICT` 및 `setLanguage('ja')` 번역 전환 함수 구현하여 `localStorage` 영구 기억 및 실시간 다국어 지원 완성. |
| **1.31** | **AI 표지 렌더링 오류** | 후보 도서 카드 표지에 텍스트가 깨져 알아볼 수 없는 네모(豆腐) 글자로 노출 | 구버전 표지 생성 로직이 남긴 5KB 미만 저품질/손상 표지 파일이, 후보 도서 보관함(`pool`) 재사용 시 유효성 검증 없이 그대로 재노출됨 | 1. `generate_candidates`의 보관함 재사용 루프에 파일 존재 여부 및 5KB 미만 크기 검증 로직 추가 → 손상 표지 감지 시 `cover_image_url`을 `None`으로 초기화하여 자동 재생성 유도.<br>2. DB에 남아있던 손상 표지 레코드 13건 정리 및 대응 파일 삭제 완료. |
| **1.32** | **표지 생성 응답 속도 저하** | AI 표지 생성이 최악의 경우 수십 초까지 지연되어 "표지 생성 중…" 로딩이 오래 지속됨 | Imagen 3(25초)·Pollinations.ai(35초) 순차 타임아웃 + 후보 3권을 `for`문으로 순차 생성하여 대기 시간이 누적됨 | 1. Imagen 3 타임아웃 25초→8초, Pollinations.ai 타임아웃 35초→10초로 단축.<br>2. `_generate_covers_background`를 `asyncio.gather`로 개편하여 후보 3권의 표지를 동시 병렬 생성하도록 변경 → 체감 대기 시간을 개별 후보 1건 수준(약 20초 이내)으로 단축. |
| **1.33** | **마감임박 목록 표지 미노출** | 홈 화면 "마감이 가까운 독서방" 목록의 소형 썸네일이 AI 표지가 있어도 항상 단색 컬러로만 표시됨 | `books.js`의 `renderDeadlineSection()`이 `deadline-mini-cover`를 항상 `book.color` 단색 배경으로만 렌더링하고 `cover_image_url`을 참조하지 않음 | `deadline-mini-cover` 스타일 계산 로직을 다른 카드(메인/상세)와 동일한 패턴으로 통일하여, AI 표지가 존재하면 소형 썸네일에도 배경 이미지로 우선 노출되도록 수정. |
| **1.34** | **AI 사회자 문장 잘림(Truncation)** | `@사회자` 멘션 응답 시 AI 사회자 문장이 중간에 잘려 문장이 끝까지 노출되지 않음 | 1. Gemini 2.5 Flash 추론 모델(Thinking Token ~950토큰) 사용 시 `maxOutputTokens: 1000` 제한으로 생성 토큰 예산 초과(MAX_TOKENS).<br>2. 파이썬 백엔드의 이전 슬라이싱 로직(`[:last_punct+1]`)이 마침표 전까지 문장을 잘라냄.<br>3. DB `chat_messages` 테이블의 `content` 컬럼이 MySQL에서 `VARCHAR(255)`로 고정되어 DataError(1406) 발생. | 1. `main.py` 내 `maxOutputTokens`를 1000 ➔ 4096으로 대폭 확장하고 글자수 제한 프롬프트 완전 해제.<br>2. 파이썬 백엔드 문장 슬라이싱 로직 제거 ➔ 미완성 부호 마감 보정으로 개편.<br>3. MySQL DB `chat_messages` 및 `past_chat_messages` 테이블의 `content` 컬럼을 `TEXT` 타입으로 마이그레이션 변경 완료. |
| **1.35** | **독자 대화 우선 모드 개편** | AI 사회자가 4개 댓글마다 자동으로 개입하여 채팅방을 주도하는 바람에 독자 간 자율 대화 흐름이 방해됨 | `main.py` 내 `msgs_since_last_mod >= 4` 주기적 자동 개입 및 일반 단어 `"사회자"` 감지 로직으로 인해 AI 개입 빈도가 과도함 | 1. `msgs_since_last_mod >= 4` 자동 개입 로직 완전 제거.<br>2. 멘션 키워드를 명시적 `@사회자`, `@moderator` 태그로 엄격 제한하여 독자 간 자율 대화를 최우선 보장하도록 개편. |
| **1.36** | **공식 반응 4종 통일 & 답장하기 버튼 UX 개선** | 1. 시드 데이터에 공식 이모지 외 다수의 기타 이모지가 혼재되어 노출됨.<br>2. 댓글 반응 팝오버의 "답장하기" 버튼이 어두워서 눈에 잘 띄지 않음 | 1. 시드 생성 스크립트 상에 기타 이모지가 임의 포함됨.<br>2. `.rx-picker-reply` 스타일이 투명 배경과 옅은 회색 글꼴(`color: #ccc`)로 스타일링되어 가독성 낮음 | 1. DB 독서방 반응 데이터를 공식 4종(`❤️` 공감해요, `🤔` 생각이 달라요, `😄` 재밌어요, `✨` 인상 깊어요)으로만 100% 엄격 재정리 완료.<br>2. `styles.css` 내 `.rx-picker-reply`를 고급스러운 웜 엠버 그라데이션, 볼드체 선명한 흰색 글꼴(`font-size: 10.5px`, `font-weight: 700`), 호버 Glow 및 확대 효과로 시각적 가독성 극대화. |
| **1.37** | **도서 상세페이지 글자 크기 최적화** | 도서 상세화면의 제목, 작가, 줄거리, 태그, 버튼 등의 글자 크기가 작아 가독성 및 시각적 몰입감이 저하됨 | `.detail-title`(22px), `.detail-author`(12px), `.detail-synopsis`(14.5px), `.btn-join`(12px) 폰트 규격이 전체 카드 대비 좁고 작게 설정됨 | `styles.css` 내 도서 제목(`24px` Bold), 작가명(`13.5px` Semibold), 줄거리 본문(`15.5px`, Line-height `1.85`), 버튼(`13.5px`), 태그/가격(`12~13px`) 및 추천사/서평(`15.5~16px`) 글자 크기 및 줄간격을 가독성 최고 비율로 시원하게 재조정 완료. |
| **1.38** | **전체 사이트 글로벌 타이포그래피 시원시원한 가독성 개편** | 사이트 전반의 마이크로 폰트(8~10px) 및 채팅/본문 폰트가 작아서 고해상도 디스플레이 및 이동식 기기에서 눈의 피로도 누적 | `styles.css` 내 타임스탬프, 뱃지, 이모지 라벨, 메타 데이터 등에 `8px`, `9px`, `10px` 극소형 폰트가 40여 곳 이상 광범위하게 지정됨 | 1. 글로벌 극소형 폰트(8~10px) 40여 곳 전수를 **`12px`~`13px`로 상향 조율**.<br>2. 독서 및 댓글 채팅 본문 폰트를 **`15px`~`15.5px`로 시원하게 확대**.<br>3. 카드 제목 및 세션 헤더 폰트를 **`18px`~`24px`로 선명화**하여 시각적 위계 구조 및 독서 몰입감 완벽 확보. |
| **1.39** | **장르별 모아보기 좌우 탐색 버튼 & 순서 개편** | 메인 화면 "장르별 모아보기"에서 좌우 버튼으로 순차 전환하고 싶은 UX 요구 반영 | 기존 탭들이 순서 규칙 없이 배치되고 좌우 탐색 버튼이 미존재함 | 1. `home.html` 및 `index_standalone.html` 내 `genre-tabs-wrapper` 상에 좌(`‹`), 우(`›`) 원형 탐색 버튼 탑재.<br>2. `books.js` 내 장르 배열 순서를 **`아카이브` ↔ `전체` ↔ `판타지` ↔ `에세이/비문학` ↔ `소설` ...** 순서로 엄격 정렬.<br>3. `navigateGenre(direction)` 순환 전환 비동기 제어 함수 구현하여 클릭 시 장르 변경, 아카이브 온디맨드 로딩 및 카드 목록 실시간 갱신 완성. |
| **1.40** | **마감 임박 D-day 배지 2줄 레이아웃 디자인 개선** | 메인 화면 "기간 임박 순서대로 보기" 목록의 D-day 배지가 좁고 답답하게 보임 | `.deadline-badge` 배지 크기(`48px`), 숫자 폰트(`18px`), 라벨(`12px`) 및 곡률(`8px`)이 이미지 대비 작고 세로 여백이 부족함 | `styles.css` 내 `.deadline-badge` 크기(`54px` x `54px`, 곡률 `12px`), 상단 큰 숫자(`22px` Bold), 하단 라벨(`11.5px` Bold `일 남음`) 2줄 레이아웃으로 시원하고 도톰하게 디자인 전면 개편 완료. |
| **1.41** | **D-day 배지 [9일/남음] 레이아웃 보정 & 도서 목차-줄거리 서사 매핑 개편** | 1. D-day 배지 상단에 숫자와 '일'이 붙지 않고 분리됨.<br>2. 도서 상세 목차가 도서의 실제 줄거리/인물과 부합하지 않고 정적인 템플릿("비밀의 문")으로 노출되는 현상 파악. | 1. HTML 상에 `<div class="deadline-days">9</div><div class="deadline-lbl">일 남음</div>`으로 나뉘어 노출됨.<br>2. `generateRichTableOfContents` 하드코딩 매핑에 신규/특수 도서 제목이 미포함되어 장르 기본 템플릿으로 Fallback 처리됨. | 1. `books.js` 및 `styles.css` 내 D-day 배지를 **상단 `9일` (숫자+일), 하단 `남음`** 레이아웃으로 완벽 보정.<br>2. `books.js` 내 `그림자를 녹이는 만년필의 시간`, `깨진 렌즈가 비춘 잃어버린 항해`, `오래된 안경 상점` 등 개별 도서의 줄거리/인물 전용 챕터 목차 탑재 및 줄거리 문장 기반 동적 서사 목차 파서 구축 완료. |
| **1.42** | **내 서재 (My Library) UI 대대적 고급 개편** | 기사의 전자책 대표 플랫폼 4종(밀리의 서재·교보eBook·리디 등) 비교를 참고하여 서재 UI 고급화 요구 | 기존 서재 헤더가 단순 텍스트 나열 위주여서 독서 리포트 및 서재로서의 직관성이 저하됨 | 1. `library.html` 및 `index_standalone.html`에 **서재 히어로 대시보드(`lib-hero-dashboard`)** 구축 (프로필 아바타 링 + 독서 레벨 뱃지 + 4대 통계 카드).<br>2. `styles.css` 내 럭셔리 대시보드, 4종 통계 카드, 세그먼트 탭 컨트롤바 및 명품 책장 섹션 스타일링 반영.<br>3. `books.js` `renderLib()` 상에 `참여 아카이브` 실시간 수치 연동 완료. |
| **1.43** | **내 서재 문학적 톤앤매너(Aesthetic Tone) 재정립** | 현대 앱 스타일의 이모지 카드 및 뱃지가 가공독서회의 따뜻하고 정갈한 문학적 분위기와 이질감을 형성 | 화려한 컬러 이모지 및 상업 앱 형태의 레벨 뱃지가 전체 사이트의 양양하고 그윽한 양식과 미스매치됨 | 1. 묵직하고 우아한 명조체 폰트(`font-size: 32px`) **내 서재** 서명 헤더 및 서정적인 보조 설명 복원.<br>2. 웜 샌드 톤(`background: #faf6f0`) 및 세로 구분선이 포함된 정갈한 4종 통계 바(`lib-stats`)로 재디자인.<br>3. 인위적 레벨 뱃지 제거 및 클래식한 우드 텍스처 책장과의 100% 미학적 조화 완비. |
| **1.44** | **다국어(한국어 ↔ 일본어) UI/UX 전면 실시간 번역 확충** | 일본어 언어 전환 시 일부 메인 텍스트만 바뀌고 상세 UI/UX 구성 요소가 한국어로 남아있음 | `I18N_DICT` 딕셔너리 및 `setLanguage` 바인딩 노드가 일부 헤더/네비 노드에만 한정되어 있음 | 1. `books.js` 내 `I18N_DICT`에 네비, 히어로, 장르 탭, 독서방 댓글/반응, 내 서재, 후보 선택, 푸터 등 40여 개 이상의 UI/UX 키 전수 등록.<br>2. `setLanguage(lang)` 호출 시 `data-i18n` 및 동적 UI 노드가 정갈한 일본어(日本語)로 100% 실시간 번역 전환되도록 구현 완료. |
| **1.45** | **『깨진 렌즈가 비춘 잃어버린 항해』 영구 샘플 독서회방 & 3일차(7.29~7.31) 날짜 분리 대화 구축** | 활성화된 독서방의 분위기를 상시 체감할 수 있는 영구 샘플 독서방 구축 요구 | 기존 10일 자동 종료 로직으로 인해 샘플 독서방이 아카이브되거나 대화가 1회성에 그치는 문제 존재 | 1. `main.py` 내 `seed_lost_voyage_book_if_needed(db)` 구축하여 **7월 29일(수), 7월 30일(목), 7월 31일(금)** 3일간 7명의 열성 독자 및 AI 사회자간 활기찬 문학 토론 대화 탑재.<br>2. 자동 종료 예외 처리(`deadline_days=9999`)로 상시 활성화 유지.<br>3. 날짜별 구분선 자동 분리 렌더링 완료. |
| **1.46** | **전체 활성 도서 샘플 독서방 4종 3일차(7.29~7.31) 생동감 넘치는 문학 토론 시드 완비** | 신규 사용자가 서비스에 진입했을 때 활성화된 독서방들의 열띤 대화 분위기를 체감할 수 있도록 샘플 독서방 대화 확충 요구 | 새로 개설된 도서의 경우 대화 기록이 없어 초기 진입 시 독서방이 비어보이는 현상 방지 | 1. `main.py` 내 `seed_fountain_pen_book_if_needed(db)` 및 `seed_faded_gaze_book_if_needed(db)` 시드 엔진 구축.<br>2. 『그림자를 녹이는 만년필의 시간』, 『닳아버린 시선에게 운명을 묻다』 등 4개 전체 활성 독서방에 **7월 29일~31일 3일간 7인 페르소나 독자 & AI 사회자 간 토론 시드** 구축 및 상시 활성화(`deadline_days=9999`) 완료. |
| **1.47** | **AI 사회자 메세지 답장/반응 비활성화 및 언급(@사회자/@닉네임) 시각적 태그 하이라이트** | 1. AI 사회자 챗 말풍선 하단에 답장/반응 버튼이 노출되는 문제.<br>2. 대화 내용 중 `@사회자` 또는 독자 닉네임 언급 시 일반 텍스트로 보여 시각적 구분이 안 되는 문제 | AI 사회자는 대화를 이끄는 보조 주체이므로 답장/반응 대상에서 제외되어야 하며, 언급 태그는 명확한 시각적 하이라이트가 필요함 | 1. `books.js` 내 `buildMsgEl` 및 `showRxPicker` 수정하여 AI 사회자 말풍선 하단 답장/반응 툴바 및 더블클릭 반응 피커 완전 비활성화.<br>2. `formatMsgText` 함수를 구현하여 `@사회자` 언급 시 웜 골드/엠버 뱃지(`background: #fef3c7`), 일반 독자 언급 시 스카이 블루 뱃지(`background: #e0f2fe`)로 자동 스타일 하이라이트 연동 완료. |

### 🔗 콘텐츠 고도화 학습 및 참고 문헌 출처 (References)
1. **국립국어원 표준국어대사전**: [https://stdict.korean.go.kr/main/main.do](https://stdict.korean.go.kr/main/main.do) — *풍부한 문학 어휘(유영, 궤적, 섭리, 공명, 잔상, 찰나, 파문 등) 추출 및 챕터명/줄거리 구성 반영*
2. **소설 감정표현 모음 (로다의 잉여생활)**: [https://rodalife20.tistory.com/473](https://rodalife20.tistory.com/473) (참고도서: *인간의 130가지 감정 표현법*, 안젤라 애커만·베카 푸글리시) — *신체적 반응, 내적 동요, 파워 동사 기법을 통한 독자 상상 리뷰 입체감 강화*
3. **현대소설 필수 개념 정리 (국어의 시작과 끝)**: [https://goodballad.tistory.com/11739250](https://goodballad.tistory.com/11739250) — *서술과 묘사, 액자식 구성, 말하기와 보여주기, 복선과 암시, 어조(해학/냉소/반어/풍자) 서사 이론 접목*
4. **글을 쉽게 쓰는 법 (3) - 소재를 찾는 5가지 방법 (Steemit)**: [https://steemit.com/kr/@nuhorizon/3-5](https://steemit.com/kr/@nuhorizon/3-5) — *독자 관심사 교집합 발상, 일상 사물 What-If 물음표 낚싯대 던지기, 완벽주의를 깬 기발하고 엉뚱한 상상력 유발 기법 접목*

---

## 💡 주요 시스템 건축 및 기술적 성과 요약

> [!NOTE]
> **모듈화 템플릿 구조 (Jinja2 Templates)**
> 메인 뼈대 `index.html`을 중심으로 `components/`(네비게이션, 글로벌 푸터)와 `pages/`(홈, 후보 생성, 도서 상세, 채팅방, 내 서재, 이용 안내 등) 모듈로 체계적으로 분리하여 가독성과 유지보수 편의성을 극대화했습니다.

> [!TIP]
> **온디맨드 REST API 아키텍처**
> 첫 진입 시 무거운 데이터 전체를 로딩하지 않고, 사용자가 클릭하거나 이동하는 시점(온디맨드)에 단일 도서 상세 API(`/api/books/{id}`) 및 아카이브 API를 호출하여 네트워크 트래픽을 아끼고 속도를 극대화했습니다.

> [!IMPORTANT]
> **후보 도서 보관함(Pool) 무작위 랜덤(Random Mix) 순환 관리**
> 3권 중 선택받지 못한 후보 도서를 DB 보관함(`pool`)으로 자동 수집하고, "다음 책 생성" 시 보관함 속 도서들 중에서 매번 무작위 랜덤(`func.random()`)으로 꺼내어 매번 다채롭고 신선한 3권 후보 조합을 보여주도록 구현했습니다.
